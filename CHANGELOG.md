# CHANGELOG — City Live / רהיטי הסיטי

יומן שינויים של הפרויקט. **חובה לעדכן בכל שינוי בקוד, מיגרציה, Edge Function, או התנהגות מוצר.**

פורמט: `[YYYY-MM-DD]` → כותרת קצרה → bullets (מה / למה / קבצים עיקריים).
הערכים החדשים ביותר בראש הקובץ.

ראה גם: `PROJECT_CONTEXT.md` (ארכיטקטורה וכללים קבועים) — כאן רק **מה השתנה**.

---

## [2026-09-22] — דשבורד WA: שני מדדים חדשים פר-נציגה בטבלת "פירוט לפי נציגה"

- **מה**: שתי עמודות חדשות בטבלת "פירוט לפי נציגה" ב"דשבורד WA":
  - **"מענה ממוצע לכלל ההודעות"** — ממוצע זמן תגובה על **כל** הודעה של
    הלקוח בפנייה (לא רק ההודעה הראשונה, כמו "תגובה מוקד"/"תגובה נציגה"
    הקיימים). מחושב מ-`zendesk_whatsapp_messages` (אותה טבלה שממנה
    נגזרים כל שאר זמני התגובה): לכל "תור" של הלקוח (או ה-handoff מהבוט,
    אם עוד לא ענו) עד תגובת הנציגה הבאה, וממוצע על הכל, נזקף למי
    שמשויכת לפנייה **עכשיו**. הודעה: מכיוון שהמנגנון שלנו סופר "מעברי
    תור" ולא כל בועת WhatsApp בנפרד (ראה הערת קוד ב-`whatsappFlip`),
    כמה הודעות רצופות של הלקוח לפני שנענו נספרות כמענה אחד, לא כמה.
  - **"מעל 10 דק'"** — פר-נציגה: כמה מהפניות שלה חיכו (או עדיין מחכות)
    מעל 10 דקות לתגובה ראשונה. קיים כבר כמספר כולל למחלקה למעלה בעמוד -
    כאן זה מפורק לפי נציגה, אותו חישוב בדיוק.
- **למה**: בקשת בעל החשבון - הדשבורד הפנימי (לא ה-TV) ישמש אותו לראות
  הכל פר-נציגה במקום אחד, כולל מדד שלא היה קיים (ממוצע על כל הודעה, לא
  רק הראשונה).
- **חשוב**: זה מספר חדש שמשפיע על מספרים שקשורים לשכר - הוסבר לבעל
  החשבון לפני הביצוע, 2026-09-22. לא הוחלף/נגע במדדים הקיימים
  (תגובה מוקד/נציגה) - זה מדד נוסף בלבד.
- **קבצים**: `src/lib/wa-dashboard.ts` (הפונקציה
  `averageMessageResponseByAgent` + שדות חדשים ב-`WaAgentSummary`),
  `src/app/api/wa-dashboard/route.ts` (שאילתה נוספת ל-
  `zendesk_whatsapp_messages`), `src/components/wa-dashboard-page.tsx`
  (שתי העמודות), `src/lib/wa-dashboard.test.ts`.

---

## [2026-09-22] — כלי אבחון: בדיקת תבנית WhatsApp לפנייה בודדת (`check_ticket`)

- **מה**: הוספת מצב `check_ticket` ל-Edge Function `sync-zendesk-tickets`:
  קריאת `POST` עם `{ "check_ticket": "<מספר פנייה>" }` בודקת דרך ה-API
  הפרטני של Zendesk (`/tickets/{id}/comments.json`, לא ה-incremental
  export) האם יש לפנייה הערת "נשלחה הודעת WhatsApp עם תבנית" - **בלי**
  לגעת ב-`zendesk_sync_state` ובלי להשפיע על הסנכרון החי בשום צורה.
- **למה**: נבנה תוך כדי החקירה של תקלת התבניות (ראה הרשומה למעלה) כדי
  לבדוק פניות ספציפיות שהופיעו תקועות ב-TV, בלי להריץ שוב backfill
  היסטורי מלא (שגרם בעבר לעצירת עיבוד בזמן אמת - פנייה #81220).
  שימש בפועל לאיתור פניות #79700, #74090, #79870 (כולן תבנית שלא
  נספרה) מול #79682/#80393/#79211 (לא קשור לתקלה).
  **פרוסה מ-22.09 (`supabase.functions.deploy`) — קומיט זה רק מסנכרן את
  ה-git להתאמה למה שכבר רץ בפרודקשן.**
- **קבצים**: `supabase/functions/sync-zendesk-tickets/index.ts`.

---

## [2026-09-22] — תיקון: שליחת תבנית WhatsApp לא נספרה כמענה נציגה

- **מה**: כששולחים ללקוחה תבנית WhatsApp (לא הודעת צ'אט חופשית), Zendesk
  לא מפעיל את אותו מנגנון שסימן "הנציגה ענתה" (אין תג
  `last_whatsapp_reply_agent`) - כך שהפנייה נשארה מסומנת "ממתינה לתגובה"
  לנצח, ו"זמן תגובה נציגה"/"זמן תגובה מוקד" מעולם לא נספרו לה, למרות
  שנשלחה הודעה אמיתית ללקוחה. דוגמה: פנייה #78940 (שירה מילר) הראתה
  123+ שעות המתנה, כי התבנית שנשלחה ב-17.09 מעולם לא נספרה.
- **הזיהוי**: Zendesk משאיר הערה פנימית קבועה בכל שליחת תבנית - "נשלחה
  הודעת WhatsApp עם תבנית: &lt;שם תבנית&gt;" (השם משתנה, התחלת המשפט
  קבועה). `via.channel = "api"` לבד לא מספיק לזיהוי (משותף גם להערות
  יומן שיחות של Aircall) - הזיהוי לפי תחילת הטקסט של ההערה, אומת ישירות
  מול Zendesk (פנייה #79061, 22.09).
- **התיקון**: כשהסנכרון (`sync-zendesk-tickets`) רואה הערה פנימית כזו,
  הוא רושם אותה כהודעת נציגה רגילה (`zendesk_whatsapp_messages`,
  direction='agent') - בדיוק כמו הודעת צ'אט - כך שכל החישובים הקיימים
  (זמן תגובה, "ממתינים לתגובה") מתעדכנים נכון מאליהם.
- **Backfill**: הרצתי ידנית תיקון על שתי הפניות שנבדקו (78940, 79061) -
  אומת: "ממתינים לתגובה" הפסיק לרוץ, זמני תגובה נרשמו נכון. בנוסף אפסתי
  את סמן הסנכרון של אירועים (`zendesk_sync_state.last_events_start_time`)
  חזרה ל-2026-09-08 (התאריך שממנו הנתונים כבר אמינים) כדי שהתיקון יחול
  אוטומטית על כל פנייה תקועה דומה מאז - התהליך רץ ברקע (אידמפוטנטי,
  upsert בלבד).
- **חשוב**: זה משפיע על מספרים שקשורים לשכר (זמני תגובה) - ככל הנראה
  **יוריד** זמני תגובה שנראו גרועים באשמת פניות שהתבנית שלהן לא נספרה.
  הוסבר לבעל החשבון לפני הביצוע, 2026-09-22.
- **קבצים**: `supabase/functions/sync-zendesk-tickets/index.ts`.

---

## [2026-09-22] — קיצור פער הסנכרון: משיכת פנייה עד היעלמות מהמסך

- **מה**: הפער בין הרגע שנציגה מושכת פנייה (משויכת לעצמה) לרגע שהיא
  נעלמת מ"ממתינים לשיוך נציגה" (TV/דשבורד) התכווץ מעד כ-90 שניות לעד
  כ-40: (1) הסנכרון מ-Zendesk (`sync-zendesk-tickets`) עבר מריצה אחת
  בדקה לשתי ריצות בדקה (קריאה מיידית + קריאה נוספת 30 שניות אחר כך,
  דרך `pg_sleep` באותה משימת cron - `net.http_post` אסינכרוני ולא
  חוסם, אז זמן הריצה בפועל של המשימה נשאר כ-30 שניות, הרבה לפני הטיק
  הבא). (2) רענון המסך (TV WA + דשבורד WA) ירד מ-30 שניות ל-10.
- **בדיקה**: אומת ישירות מול `cron.job_run_details` - משך הריצה עלה
  מ-0 שניות ל-כ-30, בדיוק כמתוכנן.
- **סיכון**: נמוך - `sync-zendesk-tickets` אידמפוטנטי (cursor + upsert),
  אז חפיפה מקרית בין ריצות לא פוגעת בנתונים. אם Zendesk יחסום (429),
  הקוד הקיים כבר עוצר בעדינות וממשיך בריצה הבאה.
- **למה**: בקשת בעל החשבון, 2026-09-22.
- **קבצים**: `supabase/migrations/20260922080000_zendesk_tickets_sync_every_30s.sql`,
  `src/components/wa-dashboard-page.tsx`, `src/components/wa-wallboard-client.tsx`.

---

## [2026-09-20] — עמוד סקרים: סינון לפי מספר הזמנה

- **מה**: נוסף שדה טקסט "מספר הזמנה" לשורת הסינון בעמוד "סקרים". הסינון
  לפי "מכיל" (התאמה חלקית, לא רק מדויקת) ומשפיע על כל המסך בדיוק כמו
  שאר הפילטרים — כרטיסי סיכום, פילוח, טבלאות וגם הפיד "משוב אחרון".
  נבדק בדפדפן: סינון לפי SO26-2007695 (מדויק) ולפי 2007695 (חלקי)
  הציגו את אותה תשובה בודדת עם ממוצעים תואמים.
- **קובץ**: `src/components/surveys-overview.tsx`.

---

## [2026-09-20] — "משוב אחרון": הצגת שם הלקוח

- **מה**: כל שורה בכרטיס "משוב אחרון" בדשבורד הראשי מציגה עכשיו את
  שם הלקוח (`survey_pending_sends.customer_name`) לפני מספר ההזמנה —
  הנתון כבר נטען מה-DB (משמש בחלון ה-drill-down), רק נוסף לתצוגת הפיד.
  נבדק מול DB לפני השינוי (SO26-2007695 → "אלירן ונטע אוחנה") ואומת
  בדפדפן אחרי הפריסה.
- **קובץ**: `src/components/surveys-overview.tsx`.

---

## [2026-09-17] — בקשת ביקורות בגוגל: כפתור "הסר נבחרים" (מרובה)

- **מה**: כפתור "הסר נבחרים" בעמוד `/surveys/reviews`, לצד "שלח בקשת
  ביקורת" — משתמש באותם checkbox-ים שכבר קיימים לבחירה מרובה, ומסיר
  את כל הנבחרים מהרשימה בבת אחת (לא רק אחד-אחד דרך "הסר" בשורה).
- ה-API (`POST /api/surveys/five-star`) תומך עכשיו גם ב-`ids` (מערך)
  וגם ב-`id` בודד (תאימות לאחור).
- נבדק מול ה-API עם 2 תשובות בדיקה: הוסרו שתיהן בבת אחת.
- **קובץ**: `src/app/api/surveys/five-star/route.ts`, `src/components/survey-google-reviews-client.tsx`.

---

## [2026-09-17] — "משוב אחרון": הצגת שעת הסקר, לא רק תאריך

- **מה**: בכרטיס "משוב אחרון" בדשבורד הראשי, כל תשובה מציגה עכשיו גם
  שעה (למשל `17.09.2026, 14:33`), לא רק תאריך.
- **קובץ**: `src/components/surveys-overview.tsx`.

---

## [2026-09-17] — הסרת לקוח מרשימת ביקורות, דפדוף במשוב אחרון, סימון נשלח

- **הסרה מרשימת 5 הכוכבים**: כפתור "הסר" בכל שורה ב-`/surveys/reviews` —
  מסמן `google_review_skipped_at` (עמודה חדשה), לא שולח כלום, הלקוח פשוט
  לא יופיע יותר ברשימה. נבדק ישירות מול ה-API (מוודא שהתשובה נעלמת
  מהרשימה אחרי הסימון).
- **מגבלת גוגל שהוסברה לגלעד**: אין דרך (API רשמי) לדעת אם לקוח ספציפי
  שקיבל קישור באמת השאיר ביקורת בגוגל — מטעמי פרטיות. אפשר לדעת רק
  אם *נשלחה* הבקשה.
- **"משוב אחרון" בדשבורד הראשי**: עבר לדפדוף (8 בעמוד, "הקודם"/"הבא",
  "עמוד X מתוך Y") כדי להציג את כל התשובות במקום רק 8 האחרונות. כל
  תשובה שכבר נשלחה לה בקשת ביקורת בגוגל מסומנת בתג "נשלחה בקשת ביקורת".
- **קובץ**: `supabase/migrations/20260917115607_survey_google_review_skip.sql`,
  `src/app/api/surveys/five-star/route.ts`, `src/components/survey-google-reviews-client.tsx`,
  `src/app/surveys/page.tsx`, `src/components/surveys-overview.tsx`.

---

## [2026-09-17] — בקשת ביקורות בגוגל: הצגת משוב חיובי/שלילי

- **מה**: בעמוד `/surveys/reviews`, מתחת לכל לקוח ברשימת ה-5 כוכבים,
  מוצג המשוב החופשי שלו (חיובי/לשיפור) — אותה עיצוב פילים כמו בפיד
  "משוב אחרון" בדשבורד הראשי.
- נבדק בדפדפן מול נתונים אמיתיים.
- **קובץ**: `src/app/api/surveys/five-star/route.ts`, `src/components/survey-google-reviews-client.tsx`.

---

## [2026-09-17] — תור שליחה: כפתור "הסר מהתור" (בלי סימון כנשלח)

- **מה**: כפתור חדש "הסר מהתור" בטאב "ממתינים" — מוחק לגמרי מ-
  `survey_pending_sends`, בלי לשלוח SMS ובלי לסמן 'sent'. עם אישור
  (`window.confirm`), לא הפיך.
- **הגנה**: ה-API (`DELETE /api/surveys/queue`) מוחק רק שורות בסטטוס
  'pending' — אי אפשר למחוק שורה שכבר סומנה 'sent' דרך הכפתור הזה,
  כדי לשמור על היסטוריית שליחות אמיתיות.
- נבדק (לא דרך הדפדפן כי window.confirm לא ניתן לאוטומציה — נבדק ישירות
  מול ה-API עם fetch מתוך הדף המחובר): מחיקת שורה 'pending' עובדת,
  ניסיון מחיקת שורה 'sent' מוחזר count=0 ולא נמחקת.
- **קובץ**: `src/app/api/surveys/queue/route.ts`, `src/components/survey-queue-client.tsx`.

---

## [2026-09-17] — תור שליחה: אישור לפני "ייצוא וסימון כנשלח" + תיקון לחיצה בטעות

- **מה קרה**: גלעד לחץ בטעות על "ייצוא וסימון כנשלח" (מייצא Excel + מסמן
  את הנבחרים כ-'sent') במקום "שלח SMS ישירות". 135 רשומות סומנו 'sent'
  בלי שנשלח בפועל שום SMS.
  - תוקן: כל 135 הרשומות (מזוהות לפי `sent_at` זהה — שליחה אחת בבת אחת)
    הוחזרו ל-'pending', `sent_at` אופס. לא נפגע אף לקוח (לא נשלח כלום
    בפועל, זה רק ייצוא).
- **תיקון מונע**: נוסף אישור (`window.confirm`) לכפתור "ייצוא וסימון
  כנשלח", תואם לאישור שכבר קיים ב"שלח SMS ישירות" — כדי שלחיצה אחת
  בטעות לא תסמן שוב לקוחות כ"נשלח" בלי SMS אמיתי.
- **קובץ**: `src/components/survey-queue-client.tsx`.

---

## [2026-09-17] — תיקון: קישורי גוגל לא התעדכנו בתפריטי השליחה אחרי שמירה

- **הבאג**: גלעד מילא ושמר את 6 קישורי הגוגל במסך ההגדרות — נשמרו נכון
  ב-DB, אבל תפריטי הבחירה (שליחה קבוצתית + שליחה ידנית) המשיכו להראות
  "(אין קישור)" כי כל אחד מהם החזיק עותק נפרד של הרשימה, ולא התרענן
  אחרי שמירה בלי רענון מלא של הדף.
- **התיקון**: הרשימה הועברה לרכיב ההורה (`SurveyGoogleReviewsClient`)
  כמקור אמת יחיד; מסך ההגדרות מקבל אותה כ-prop ומפעיל רענון של ההורה
  אחרי כל שמירה. נבדק בדפדפן: מחיקת קישור → נשמר → תפריט הבחירה מציג
  מיידית "(אין קישור)" בלי רענון דף, ואז שוחזר הקישור המקורי.
- **קובץ**: `src/components/survey-google-reviews-client.tsx`.

---

## [2026-09-17] — עמוד סקרים: עדכון הודעת ההזמנה, בקשת ביקורות בגוגל, החרגת תשובה מהממוצע

- **הודעת הסקר הראשונה** (`survey_message_template`, id='default') עודכנה לנוסח החדש
  של גלעד. נבדקה בפועל בשליחת SMS אמיתי ל-4 מספרי בדיקה (גלעד טובול, רומי בלילטי,
  בן אלפסי, יוני אלפסי) — אושרה, נשלחה.
  - ניסיון לשנות את שם השולח ב-InfoU ל"רהיטי הסיטי" (עברית) נכשל —
    InfoU מחזיר `Invalid sender identification`; שם שולח בעברית דורש
    רישום/אישור מראש מול InfoU. נשאר `RhityCity` בינתיים.
- **עמוד חדש `/surveys/reviews`** ("בקשת ביקורות בגוגל", עמוד הרשאה נפרד
  `surveys-reviews`):
  - מציג לקוחות שנתנו **5 בדיוק בכל שלושת התחומים** (לא רק ממוצע), שעדיין
    לא נשלחה להם בקשת ביקורת (`google_review_requested_at is null`).
  - בחירה: מספר ("50 אחרונים") או ידנית, כמו בתור השליחה.
  - קישור הביקורת בגוגל נבחר **לפי סניף אחד לכל השליחה כולה** — לא לפי
    הסניף שבו כל לקוח בפועל קנה (החלטת בעלים מפורשת). 6 קישורים (לפי
    ראשון/פ״ת/ירושלים/אשדוד/ב״ש/חיפה) ניתנים לעריכה ישירות במסך
    ההגדרות שבראש העמוד — לא צריך לשלוח אותם בצ'אט.
  - שליחה ישירה דרך InfoU (כמו תור השליחה), עם תבנית הודעה נפרדת
    (`survey_message_template` id='google_review').
  - **שליחה ידנית ללקוח בודד**: תיבת טקסט להזנת מספר הזמנה או טלפון +
    בחירת קישור סניף, ושליחה ישירה — לא דורש סקר שמולא או ציון 5 בכלל
    (`src/app/api/surveys/google-review-send-manual/route.ts`).
- **החרגת תשובה מהממוצע**: בכל תשובה (בפיד "משוב אחרון" ובחלון ה-drill-down
  של סוכן/מוביל) יש כפתור "החרג מהממוצע" עם סיבה (טעות / לא רלוונטית
  לתחום). תשובה מוחרגת נשארת במערכת (לא נמחקת) ונספרת עדיין כ"תשובה
  שהתקבלה", אבל יוצאת מכל חישוב ממוצע/התפלגות (כללי, לפי תחום, סניף,
  סוכן, מוביל).
- **קובץ**: `supabase/migrations/20260917094912_survey_google_reviews_and_exclude.sql`,
  `src/app/surveys/reviews/page.tsx`, `src/components/survey-google-reviews-client.tsx`,
  `src/app/api/surveys/google-links/route.ts`, `src/app/api/surveys/five-star/route.ts`,
  `src/app/api/surveys/google-review-send/route.ts`, `src/app/api/surveys/google-review-send-manual/route.ts`,
  `src/app/api/surveys/responses/route.ts`,
  `src/components/surveys-overview.tsx`, `src/lib/app-pages.ts`, `src/components/sidebar.tsx`,
  `supabase/functions/admin-users/index.ts` (עמוד הרשאה חדש `surveys-reviews`).

---

## [2026-09-16] — "משוך עכשיו" הופעל מחדש: SHAY_SDATE, כיסוי חלקי מכוון

- **מה**: לאחר בדיקה מדוקדקת (ראה הרשומה למטה) הוחלט להפעיל את הכפתור
  מחדש עם היקף מצומצם ומכוון, בהנחיית גלעד: סינון לפי `ORDSTATUSDES eq
  'סופקה'` (התאמה מדויקת בלבד, לא &quot;סופקה חלקית&quot;) וגם `SHAY_SDATE`
  (שדה תאריך התיאום) בטווח שנבחר — במקום `STATUSDATE` הישן.
- **מה זה כן תופס**: נבדק בפועל מול 14.09 — 151 הזמנות תואמות (אחרי
  סינון סניפים), לעומת 19 עם הגישה הקודמת. 0 זליגות לסניפי שירות, 0
  הודעות ריקות, 0 טלפונים לא תקינים.
- **מה זה מכוון לא תופס (כ-30%)**: הזמנות בסטטוס &quot;סופקה חלקית&quot;,
  והזמנות בלי SHAY_SDATE בכלל — נמצא שאלה בעיקר הזמנות טהורות של
  ארונות/סלונים (משפחות מוצר 7/71/72), שכנראה לא עוברות תהליך תיאום
  הובלה רגיל. **אלה לא הולכים לאיבוד** — &quot;שלח בקשה (ברקע)&quot; (שגרת
  הענן השעתית, שמשתמשת במקור הנכון `priority_deliveries`) ממשיכה לתפוס
  את כל מה ש-&quot;משוך עכשיו&quot; מפספס, בלי כפילויות (ייחודיות לפי
  order_number).
- **קובץ**: `supabase/functions/priority-pull-now/index.ts`,
  `src/components/survey-priority-pull-form.tsx`.

---

## [2026-09-16] — "משוך עכשיו" הושבת: באג בזיהוי תאריך אספקה

- **הבאג**: גלעד דיווח שמשיכה ל-14.09 מצאה רק 19 הזמנות, לא הגיוני לחודש.
  בדקתי מול `priority_deliveries` (הכלי הקיים, אמין) — יש בפועל 310
  משלוחים אמיתיים ל-14.09 בלבד. הסיבה: "משוך עכשיו" סינן לפי `STATUSDATE`
  על ORDERS, אבל השדה הזה מתעדכן כל פעם שהסטטוס נוגע (כולל עדכוני רקע),
  לא בהכרח בתאריך האספקה בפועל — בדיקה ישירה של הזמנה אמיתית מ-14.09
  הראתה `STATUSDATE` = 16.09 (היום), לא 14.09. תאריך האספקה האמיתי נשמר
  במסמך משלוח נפרד בפריוריטי, לא כשדה על ORDERS עצמו — לא מצאתי (עדיין)
  את שם ה-entity/OData המדויק שלו.
- **מה זה לא**: אין נתונים שגויים/מזוהמים בתור — ה-19 שנכנסו הם הזמנות
  אמיתיות עם תאריך נכון, פשוט חסר (undercoverage), לא שגוי.
- **התיקון הזמני**: כפתור "משוך עכשיו" **הושבת** בממשק (עם הודעה מוסברת)
  עד שאמצא את השדה/המסמך הנכון. "שלח בקשה (ברקע)" — הדרך הישנה דרך שגרת
  הענן שמשתמשת ב-`priority_deliveries` האמיתי — ממשיכה לעבוד נכון
  ונשארת ברירת המחדל.
- **צעד הבא המוצע**: לשאול את מי שהקים את חיבור ה-MyPriority (ראז גולן
  / nf-digital) איזה מסמך/OData entity מזין את `priority_deliveries`,
  כדי לחבר את "משוך עכשיו" לאותו מקור נכון במקום STATUSDATE.
- **קובץ**: `src/components/survey-priority-pull-form.tsx`.

---

## [2026-09-16] — חיבור ישיר לפריוריטי: "משוך עכשיו" (בלי המתנה, בלי תור)

- **מה**: כפתור חדש "משוך עכשיו" בטופס המשיכה מפריוריטי (עמוד סקרים).
  בניגוד לכפתור "שלח בקשה" הקיים (כותב שורה ל-`survey_priority_pull_requests`
  וממתין לשגרת הענן השעתית), הכפתור החדש קורא ישירות ל-API של פריוריטי
  (OData) ומכניס לתור באופן מיידי וסינכרוני — בלי המתנה ובלי תלות בשגרת
  ענן. הכפתור הישן נשאר כגיבוי.
- **חיבור לפריוריטי**: גלעד הגדיר ב-Supabase (Edge Function project
  secrets — **לא** Vault, לא בגיט) שלושה ערכים: `priority_user`,
  `priority_password`, `priority_url`. גילינו/אימתנו דרך `priority_url`
  (שהכיל דוגמת קריאה מלאה) את מבנה ה-API: OData על
  `https://city-priority.d-m.org.il/odata/Priority/tabula.ini/recity/ORDERS`,
  Basic Auth. שדות רלוונטיים: `CUSTNAME` (סניף מאושר), `BRANCHNAME` (סינון
  סניפי שירות), `AGENTNAME`, `CDES` (שם לקוח), `SHIPPERNAME` (מוביל),
  `STATUSDATE` (תאריך אספקה), `ORDSTATUSDES eq 'סופקה'` (סטטוס), וטלפון
  דרך `$expand=SHIPTO2_SUBFORM` → `PHONENUM`/`CELLPHONE` (השדה
  `SHAY_PHONENUM` על ORDERS חסום הרשאות למשתמש ה-API הזה).
- **Edge Function חדש**: `priority-pull-now` (verify_jwt=false, מאומת עם
  header `x-priority-secret` מול `get_priority_pull_secret()` — סוד חדש
  ב-Vault, מוענק ל-`authenticated` בלבד, כדי שרק שרת ה-Next.js של האפליקציה
  עצמה יוכל להפעיל אותו). מריץ את אותה לוגיקת סינון בדיוק כמו שגרת הענן
  השעתית (CUSTNAME מאושר + החרגת BRANCHNAME של סניפי שירות, עם
  `includeServiceCalls` אופציונלי), כותב ישירות ל-`survey_pending_sends`.
  נבדק בפועל על 16.09: 134 הזמנות תואמות, 120 נוספו לתור (14 כבר היו שם
  משגרת הענן), 0 זליגות לסניפי שירות, 0 טלפונים לא תקינים.
- **קובץ**: `supabase/functions/priority-pull-now/index.ts`,
  `supabase/migrations/20260916122418_priority_pull_secret.sql`,
  `src/app/api/surveys/priority-pull-now/route.ts`,
  `src/components/survey-priority-pull-form.tsx`.

---

## [2026-09-16] — תיקון זליגת סניפי שירות לסקר + סינון ו-drill-down בדשבורד

- **הבאג**: הסינון (גם במשיכות ידניות וגם בשגרת הענן השעתית) בדק רק
  את `CUSTNAME` (הסניף "הביתי" של הלקוח) מול רשימת הסניפים המאושרים,
  ולא בדק את `BRANCHNAME` (הסניף/תור שבפועל טיפל בעסקה הספציפית).
  זה גרם להזמנות שירות/חלפים (למשל `SO26-S005788` — "שירות חלקים",
  TOTPRICE=0) לזלוג לתור הסקר ואף להישלח כ-SMS אמיתי ללקוחות.
- **התיקון**:
  - נמחקו מ-`survey_pending_sends` כל 35 ההזמנות המזוהות כסניפי שירות
    (BRANCHNAME 10 או 16 — לפי קידומת מספר הזמנה `SO26-S005xxx` /
    `SO26-16xxxxx`, מאומת ב-100% מול פריוריטי). 4 מתוכן כבר היו עם
    תשובה אמיתית בסקר (`survey_responses`) — נמחקו גם הן, כדי שלא
    יעוותו את הממוצעים בדשבורד.
  - שגרת הענן השעתית (`Survey Priority pull-request drain`) עודכנה
    לבדוק גם את `BRANCHNAME` מול רשימת 11 קודי סניפי השירות
    (10,11,12,13,14,16,17,18,19,23,24 — לפי הקובץ שהתקבל מגלעד).
  - נוספה עמודה `include_service_calls` ל-`survey_priority_pull_requests`
    + תיבת סימון "כלול גם קריאות שירות" בטופס המשיכה מפריוריטי, כך
    שאפשר לבחור במפורש למשוך גם קריאות שירות כשצריך (ברירת מחדל: לא).
- **דשבורד סקרים**: נוספו סינונים (סניף / סוכן / מוביל / טווח תאריכים)
  שמשפיעים על כל הנתונים בעמוד, ו-drill-down בלחיצה על שורת סוכן/מוביל
  שפותח חלון עם כל התשובות הספציפיות שלו (הזמנה, לקוח, טלפון, ציון,
  משוב). הדף עבר ל-client component כדי לתמוך בכך.
- **קובץ**: `supabase/migrations/20260916100306_survey_pull_service_calls_toggle.sql`,
  `src/app/surveys/page.tsx`, `src/components/surveys-overview.tsx`,
  `src/components/survey-priority-pull-form.tsx`,
  `src/app/api/surveys/priority-pull/route.ts`, ושגרת הענן עצמה
  (`trig_01HCDTc2K7ggDMGbTvhijnDa`, לא בגיט).

---

## [2026-09-16] — תור שליחה: שליחת SMS ישירה דרך InfoU (בלי Excel)

- **מה**: כפתור חדש "שלח SMS ישירות" ב-"תור שליחה", לצד ה-ייצוא ל-
  Excel הקיים. קורא ל-InfoU SMS API (`POST
  https://capi.inforu.co.il/api/v2/SMS/SendSms`) — קריאה נפרדת לכל
  נמען (כי כל לקוח מקבל הודעה שונה לגמרי, לא רק שם מוחלף בתבנית),
  עם אישור `confirm()` לפני שליחה בפועל (בלתי הפיך). על הצלחה מסמן
  את השורה כ-`sent`; כשלונות מדווחים בנפרד ולא מסומנים.
  - `User`+`Token` של InfoU (Basic Auth) נשמרים ב-Supabase Vault
    (`infou-api-user`, `infou-api-token`) — **לא** בקוד ולא כ-env
    גלוי. נגישים רק דרך פונקציית RPC `public.get_infou_credentials()`
    (security definer, מוגבל ל-`authenticated`).
  - Sender שנקבע: `"RhityCity"` (ללא רווח — InfoU אוסר רווחים בשם
    השולח, ומגביל ל-11 תווים).
  - נבדק בפועל מול ה-API האמיתי לפני הפריסה (הודעת בדיקה לטלפון
    הבעלים, `StatusId: 1` הצלחה).
- **למה**: בקשת הבעלים לשלוח סקרים ישירות מהדשבורד בלי לעבור דרך
  ייצוא/הדבקה ידנית ב-InfoU בכל פעם.
- **קבצים**: `supabase/migrations/20260916130000_infou_credentials_rpc.sql`,
  `src/app/api/surveys/send-sms/route.ts`, `src/components/survey-queue-client.tsx`.

---

## [2026-09-16] — סקרים: ייבוא אוטומטי מפריוריטי לפי טווח תאריכים

- **מה**: עד עכשיו שליפת לקוחות שסופקו מפריוריטי לתור הסקרים דרשה
  שהבעלים יבקש מקלוד להריץ את זה ידנית בסשן. עכשיו יש כפתור במסך
  "סקרים" (מתחת לכפתורי הפעולה) שפותח טופס תאריכים ("מתאריך" / "עד
  תאריך") וכותב בקשה לטבלה חדשה `survey_priority_pull_requests`.
  Routine בענן (לא חלק מהאתר עצמו) עם גישה ל-Supabase MCP ול-MyPriority
  MCP רץ **כל שעה בשעה עגולה**, מרוקן בקשה אחת ממתינה: שולף תעודות
  משלוח + הזמנות בסטטוס "סופקה" מפריוריטי, מסנן לפי `CUSTNAME`
  (קוד סניף/ערוץ — **לא** `BRANCHNAME`) מול הסניפים המאושרים
  (2,3,4,5,6,7,8,20), מוסיף ל-`survey_pending_sends` עם
  `on conflict (order_number) do nothing`, ומעדכן את הבקשה לסטטוס
  `done`/`error` עם ספירת תואמים/נוספו וסיכום קצר. הדשבורד מציג את 10
  הבקשות האחרונות וסטטוס שלהן (ממתין/מתבצע/הושלם/שגיאה), מתרענן כל 30
  שניות.
  - `token` על `survey_pending_sends` קיבל `default gen_random_uuid()`
    כדי שה-routine יוכל להכניס שורה בלי לייצר UUID בעצמו (ה-`message_text`
    מתמלא אחר כך ב-UPDATE שמטמיע את ה-token שנוצר בקישור).
  - עמודת `delivered_at` (date) נוספה ל-`survey_pending_sends`
    ומוצגת כעמודה חדשה ("סופק ב-") במסך "תור שליחה".
  - ה-routine **לא** רץ מיידית עם לחיצת הכפתור — המינימום שהמערכת
    מאפשרת לבדיקה אוטומטית הוא פעם בשעה, וזה תואם ציפייה שהוסברה
    לבעל החשבון לפני הבנייה.
- **למה**: בקשת בעל החשבון להפסיק להזדקק לקלוד בכל פעם שרוצים למשוך
  לקוחות חדשים מפריוריטי — "כפתור שאומר לקלוד להביא את הנתונים",
  בלי לדרוש פרטי API ישירים לפריוריטי מהאתר עצמו.
- **קבצים**: `supabase/migrations/20260916100000_survey_priority_pull_requests.sql`,
  `supabase/migrations/20260916110000_survey_token_default.sql`,
  `src/app/api/surveys/priority-pull/route.ts`,
  `src/components/survey-priority-pull-form.tsx`,
  `src/components/surveys-overview.tsx`,
  `src/components/survey-queue-client.tsx`,
  `src/app/api/surveys/queue/route.ts`. ה-routine עצמו מנוהל ב-
  claude.ai/code/routines (לא קובץ בריפו) — שם `Survey Priority
  pull-request drain (hourly)`.

---

## [2026-09-15] — מודול סקרי שביעות רצון (חדש)

- **מה**: מודול חדש לגמרי לסקר שביעות רצון ללקוח, נשלח **פעם אחת לכל לקוח**
  אחרי שכל מסע ההזמנה הסתיים (מכירה → תיאום → אספקה), עם 3 דירוגי כוכבים
  (מוכר/סניף, תיאום/משרד, מוביל) ו-2 שדות פידבק חופשי (חיובי/שלילי).
  - טבלאות חדשות: `survey_branches`, `survey_coordinators`, `survey_movers`,
    `survey_message_template`, `survey_pending_sends`, `survey_responses`
    (RLS: `for all to authenticated using(true)`, בלי מדיניות anon — הגישה
    הציבורית עוברת רק דרך edge function עם service role).
  - עמודת `profiles.branch_id` נוספה (עדיין לא נאכפת ב-RLS — "branch
    manager" role נדחה במכוון ל-fast-follow עתידי).
  - עמוד ציבורי `/s/[token]` (בלי login) — עוצב לפי מיתוג rcity.co.il
    (Rubik, נייבי #18376C, אדום #E63447), בעוד שהדשבורד הפנימי (`/surveys`,
    `/surveys/import`, `/surveys/queue`) עוצב לפי מערכת העיצוב הפנימית
    הקיימת של City Live. `src/proxy.ts` עודכן לאפשר גישה ל-`/s/` בלי
    אימות (matcher exclusion).
  - Edge function `survey-public` (GET/POST, `verify_jwt=false`) מגיש את
    הסקר ומקבל תשובות; כולל CORS headers מפורשים.
  - מסך ייבוא (`/surveys/import`) — קובץ Excel עם שמות עמודות בעברית,
    יוצר טוקן ייחודי וקישור לכל לקוח.
  - מסך תור שליחה (`/surveys/queue`) — סינון ממתינים/נשלחו/הכל, ייצוא
    Excel (טלפון + הודעה מוכנה ל-InfoU) עם סימון אוטומטי כ"נשלח", וביטול
    שליחה ידני.
  - מקור נתונים נוכחי לתור: משיכה חצי-ידנית מ-Priority דרך MyPriority MCP
    (session בלבד, לא אינטגרציה חיה) — הזמנות בסטטוס "סופקה" **וגם** עם
    תעודת משלוח בפועל בטווח תאריכים, לפי סניפי לקוח מאושרים
    (2,3,4,5,6,7,20,8). קוד 8 = "אתר אינטרנט" (הזמנות אתר), קודים 10/16
    ("סניפי שירות") מוחרגים במכוון לעת עתה.
  - עוקב גם אחרי סניף וגם אחרי סוכן/ת המכירות (`AGENTNAME` מפריוריטי)
    כמטא-דאטה על כל שורה בתור.
  - נרשם ב-`app-pages.ts` / `sidebar.tsx` / `ALL_PAGES`+`pageLabel` ב-
    `admin-users` (עמודים חדשים חסומים כברירת מחדל ל-admin עד הענקת
    גישה מפורשת ב"ניהול משתמשים").
- **למה**: בקשת בעל החשבון למדוד שביעות רצון לקוח לאורך כל תהליך ההזמנה
  (מכירה, תיאום, אספקה) בלי לגעת בניתוח ביקורות גוגל.
- **קבצים עיקריים**: `supabase/migrations/20260915090000_survey_module_core.sql`,
  `supabase/migrations/20260915100000_survey_agent_name.sql`,
  `supabase/functions/survey-public/index.ts`, `src/app/s/[token]/page.tsx`,
  `src/components/survey-form.tsx`, `src/app/surveys/**`,
  `src/components/survey-import-client.tsx`,
  `src/components/survey-queue-client.tsx`, `src/lib/survey-export.ts`,
  `src/proxy.ts`, `src/lib/app-pages.ts`, `src/components/sidebar.tsx`,
  `supabase/functions/admin-users/index.ts`.

---

## [2026-09-15] — תיקון הרשאות: משתמשת שאינה admin לא ראתה את "ממתינים לשיוך נציגה"

- **מה**: מדיניות ה-RLS על `zendesk_tickets` קבעה מי מותר לה לראות פנייה
  לפי **מחלקת הנציגה המשויכת** (`agents.department_id` דרך `agent_id`).
  לפנייה שעדיין לא שויכה `agent_id` הוא null, כך שהתנאי הזה אף פעם לא
  התקיים - ולכן כל משתמשת שאינה admin (למשל תפקיד viewer) לא ראתה **בכלל**
  את "ממתינים לשיוך נציגה", לא בדשבורד ולא ב-TV. Admin עוקף את הבדיקה
  ולכן לא הבחין בבעיה.
- **התיקון**: המדיניות עכשיו בודקת לפי **קבוצת הניתוב של הפנייה ב-Zendesk**
  (`zendesk_group_departments`) - בדיוק כמו שכל שאר המערכת (ה-API,
  `wa_agent_daily`, `wa_department_daily_interactions`) כבר קובעת מחלקה של
  פנייה. נבדק ישירות מול בסיס הנתונים בהדמיית ההרשאה של משתמשת viewer -
  לפני התיקון 0 פניות בתור, אחרי התיקון המספר האמיתי, ובלי דליפה למחלקה
  אחרת.
- **למה**: התגלה מדיווח בעל החשבון - TV הראה 15 ממתינים למשתמש admin ו-0
  לאותו URL בדיוק תחת משתמשת viewer (s@gmail.com). באג הרשאות קיים,
  לא קשור לשינויים האחרונים.
- **קבצים**: `supabase/migrations/20260915090000_zendesk_tickets_rls_by_group_department.sql`.

---

## [2026-09-15] — TV WA: נציגה "בהפסקה" בכתום, ומהבהבת לפי עומס אמיתי

- **מה**: כרטיס נציגה "בהפסקה" ברצועת "זמינות עכשיו" עכשיו בגוון כתום
  (כמו הצבע שכבר קיים לסטטוסים מותאמים אישית בסקציה המלאה למטה), ומהבהב
  כשיש תור חי **והעומס האמיתי שלה** (`messaging_work_items` מול
  `messaging_max_capacity`) לא מלא — למרות ש-Zendesk לא מנתב אליה פניות
  חדשות בעצמו כשהיא לא מקוונת (`freeMessagingSlots` תמיד 0 עבורה), מנהלת
  עדיין יכולה לדחוף אותה ידנית אם יש לה בפועל מקום. בקשת בעל החשבון,
  2026-09-15.
- **קבצים**: `src/components/wa-wallboard-client.tsx`.

---

## [2026-09-15] — תיקון: נציגה "בהפסקה" לא הופיעה ברצועת "זמינות עכשיו"

- **מה**: ברצועה הקומפקטית ב-TV WA (מהפרסום הקודם היום) נציגות "בהפסקה"
  לא הופיעו בכלל, למרות שתוכננו להופיע (מעומעם, בלי הבהוב). הסיבה: הקוד
  חיפש סטטוס שהמחרוזת שלו היא בדיוק `"away"`, אבל בפועל ה-status
  שמגיע מ-Zendesk לסטטוס מותאם אישית הוא השם עצמו (למשל "הפסקה"), לא
  המילה away. תוקן להשוות "כל מה שהוא לא online/transfers_only/offline"
  במקום להשוות למחרוזת away, והתווית מוצגת עכשיו לפי השם האמיתי של
  הסטטוס (`agentStatusLabel`) במקום מחרוזת קבועה.
- **קבצים**: `src/components/wa-wallboard-client.tsx`.

---

## [2026-09-15] — "ביצועי WA": "לקוחות שדיברנו איתם" (אינטראקציה יומית, לפי מחלקה)

- **מה**: מדד חדש ב"ביצועי WA" - כמה פניות (=לקוחות) קיבלו לפחות הודעה
  אחת מנציגה באותו יום, **לא משנה מתי הפנייה עצמה נפתחה** (שונה מ"פניות"
  שסופר לפי יום הפתיחה). מוצג כקובייה בסיכום (עם השוואה לתקופה הקודמת)
  וכעמודה נוספת בטבלת "לפי יום". מבוסס על טבלת ההודעות
  `zendesk_whatsapp_messages` (direction='agent'), שקיימת רק מ-2026-09-09 -
  לפני התאריך הזה מוצג "—"/הערה במקום 0.
- **איך**: טבלה חדשה `wa_department_daily_interactions` (יום, מחלקה,
  ספירת לקוחות ייחודיים), מחושבת ע"י `recompute_wa_department_daily_interactions`
  ומתעדכנת ב-pg_cron כל 10 דקות (+ ריצה לילית על 31 יום אחורה) - אותו
  דפוס בדיוק כמו `wa_agent_daily`.
- **תיקון RLS תוך כדי**: הטבלה נוצרה עם RLS מופעל (ברירת המחדל בפרויקט)
  אבל בלי מדיניות קריאה, כך שכל קריאה מאומתת החזירה 0 שורות בשקט (ללא
  שגיאה) - זוהה מיד בבדיקה ותוקן עם אותה מדיניות "מוגבל למחלקה" שיש
  ל-wa_agent_daily.
- **למה**: תצוגה בלבד - לא משפיע על שום ממוצע/חישוב קיים. בקשת בעל
  החשבון, 2026-09-15.
- **קבצים**: `supabase/migrations/20260915080000_wa_department_daily_interactions.sql`,
  `supabase/migrations/20260915081500_wa_department_daily_interactions_rls.sql`,
  `src/lib/wa-history.ts`, `src/app/api/wa-history/route.ts`,
  `src/components/wa-history-page.tsx`.

---

## [2026-09-15] — TV WA: רצועת "זמינות עכשיו" למעלה + הבהוב כשיש תור

- **מה**: רצועה קומפקטית חדשה מיד אחרי הקוביות העליונות (לפני כרטיס
  המחלקה) — כרטיסים קטנים, נציגות "מקוונות" **וגם** נציגות "בהפסקה"
  (away; אלה מסומנות בעמום עם התווית "בהפסקה", לא מהבהבות). נציגה "לא
  מקוונת" (offline) לא מופיעה ברצועה הזו. כרטיס נציגה מקוונת עם מקום פנוי
  **מהבהב** (Tailwind `animate-pulse`) כשיש פנייה ב"ממתינים לשיוך נציגה"
  (התור החי, בשעות הפעילות) - כדי שמנהלת שמסתכלת על המסך תדע מיד את מי
  לדחוף לקחת אותה. הסקציה המלאה "זמינות נציגות" (כל הסטטוסים, קיבולת,
  "נפתרו היום") נשארת במקומה למטה כמו היום — זו תוספת, לא תחליף.
- **למה**: תצוגה בלבד — לא משפיע על שום חישוב/ממוצע/שכר. בקשת בעל החשבון,
  2026-09-15.
- **קבצים**: `src/components/wa-wallboard-client.tsx`.

---

## [2026-09-15] — "ניהול משתמשים": הודעה ברורה כש-session פג במקום שגיאת JSON

- **מה**: כשה-session פג בזמן שהיית משתמש/ת בדף "ניהול משתמשים", הבקשות ל-
  `/api/users` (טעינה/שמירה/מחיקה) עברו הפניה (307) אוטומטית ל-`/login` על
  ידי `proxy.ts` (הגנת הראוטים), והדף ניסה לפרסר את ה-HTML של מסך ההתחברות
  כ-JSON — מה שהציג את השגיאה הגולמית `Unexpected token '<', "<!DOCTYPE
  "... is not valid JSON` במקום הודעה מובנת.
- **התיקון**: זיהוי המקרה הזה (`response.redirected` לכתובת שמכילה
  `/login`) והצגת הודעה ברורה ("ההתחברות פגה - מעביר אותך למסך
  ההתחברות...") + ניתוב אוטומטי ל-`/login`, במקום לנסות לפרסר HTML כ-JSON.
- **למה**: חוויית משתמש בלבד — אין השפעה על נתונים, חישובים, או הרשאות.
- **קבצים**: `src/app/users/users-management-client.tsx`.

---

## [2026-09-14] — דשבורד WA + TV WA: "ממתין למענה" בזמן אמת, ותיקון "0 שניות" בירוק

- **"ממתין למענה"/"סה״כ בפנייה" ברשימה האדומה ("ממתינים לתגובה") עוברים
  לזמן אמת** (שעון קיר), במקום לעצור מחוץ לשעות הפעילות כמו כל שאר
  הזמנים — בדיוק כמו שהתור ("ממתינים לשיוך נציגה") כבר עובד. בקשת בעל
  החשבון: "הלקוח עדיין מחכה לתגובה שלנו גם אם הן לא עובדות". זה משפיע
  **רק** על התצוגה החיה וצבעי הדחיפות (3/7/10 דק') בסקציה הזו — לא על
  אריחי היעד למעלה ("תגובה ראשונה", נשארים שעות עבודה בלבד לפי בקשה
  מפורשת), ולא על שום ממוצע שמור/שכר.
- **תיקון לרשימה הירוקה מאתמול** ("ענינו, ממתינות ללקוח"): כמה שורות
  הציגו "0 שניות" באופן מטעה — התברר שההודעה של הלקוח הייתה בפועל
  *אחרי* הודעת הנציגה (לא זוג תואם), אז החישוב נתן משך זמן שלילי
  שהתעגל ל-0. עכשיו מוצג "—" במקרים כאלה.
- קובץ: `src/lib/wa-dashboard.ts` (`currentlyWaiting` עבר לשעון קיר;
  guard חדש על `lastReplySeconds`), `src/app/api/wa-dashboard/route.ts`,
  `src/components/wa-dashboard-page.tsx`, `src/components/wa-wallboard-client.tsx`,
  `src/lib/wa-dashboard.test.ts` (טסט חדש: זמן אמת חוצה סוף שבוע).

---

## [2026-09-14] — דשבורד WA + TV WA: רשימה ירוקה "ענינו, ממתינות ללקוח" ליד "ממתינים לתגובה"

- בקשת בעל החשבון: ליד רשימת הפניות שמחכות לתגובת הנציגה (אדום), נוספה
  רשימה ירוקה מקבילה — פניות פתוחות בסטטוס "בהמתנה" (pending) שהנציגה
  כבר ענתה בהן, והכדור אצל הלקוח. **תצוגה בלבד** — לא משפיע על שום ממוצע
  או חישוב קיים.
- **הזמן בשורה הירוקה** אינו "זמן תגובה" כללי — זה ספציפית כמה זמן לקח
  לנציגה לענות **להודעה האחרונה של הלקוח** (מ-`last_customer_message_at`
  עד `last_agent_message_at`), בדיוק כמו שבעל החשבון ביקש: "לקוח שלח
  ב-8:05, היא ענתה ב-8:10 → 5 דק'".
- בדשבורד WA: טבלה ירוקה נפרדת מתחת לטבלה האדומה. ב-TV WA: תת-רשימה
  ירוקה בתוך הקבוצה הקיימת של כל נציגה (רק לנציגות שכבר יש להן פניות
  אדומות מוצגות).
- קבצים: `src/lib/wa-dashboard.ts` (טיפוס `WaPendingTicket` +
  `pendingReplies` ב-`WaDashboardPayload`), `src/app/api/wa-dashboard/route.ts`
  (שאילתת status='pending' חדשה), `src/components/wa-dashboard-page.tsx`,
  `src/components/wa-wallboard-client.tsx`.

---

## [2026-09-14] — TV WA: כרטיס המחלקה (תגובה מוקד/נציגה) עלה מעל "ממתינים לשיוך נציגה"

- בקשת בעל החשבון (בשני שלבים): כרטיס הסטטיסטיקה של המחלקה (תגובה
  מוקד/נציגה, עד סגירה) הוזז מתחתית המסך (אחרי "זמינות נציגות") לראש
  המסך — מיד אחרי אריחי היעדים (מעל 3/7/10 דק'), לפני "ממתינים לשיוך
  נציגה" ו"ממתינים לתגובה" — כדי שיהיה גלוי מיד בלי לגלול.
- קובץ: `src/components/wa-wallboard-client.tsx` (העברת JSX בלבד, אין
  שינוי בנתונים או בחישוב).

---

## [2026-09-14] — "ביצועי WA": אותו פיצול "מוקד"/"נציגה" גם ברשומה השמורה

- המשך ישיר לפיצול "זמן תגובה" שנוסף היום לדשבורד WA ול-TV WA (למטה) —
  עכשיו גם `recompute_wa_agent_daily` (ה-SQL מאחורי "ביצועי WA") שומר את
  שני המספרים: "זמן תגובה מוקד" (הקיים, ללא שינוי) ו"זמן תגובה נציגה"
  (חדש, מרגע `assigned_to_agent_at`).
- **בשונה מהתיקון הקודם בפונקציה הזו** (נענו/נסגרו לפי מתי זה קרה) — כאן
  **לא צריך תאריך חיתוך**: זו תוספת טהורה של שתי עמודות חדשות
  (`agent_response_seconds_sum`, `agent_responded_count`), אף עמודה קיימת
  לא משתנה בשום יום, עבר או עתיד. לכן הורצה הרצה חוזרת מלאה על כל הטווח
  הקיים (מ-01.08.2026, אותו טווח כמו הבאקפיל המקורי) בלי חשש.
- **נבדק מול דאטה חי:** ירין אללוף, 14.09 — מוקד ממוצע 45:57 מול נציגה
  ממוצע 08:00, זהה למה שכבר נראה בדשבורד WA. ימים לפני 09.09 (לפני
  שהתחילו להישמר transitions) מציגים כמעט תמיד "—" בעמודת "נציגה" — צפוי.
- קבצים: `supabase/migrations/20260914140000_wa_agent_daily_response.sql`
  (הורחבה `recompute_wa_agent_daily` הקיימת, לא job חדש; הוחל ישירות
  ב-Supabase דרך MCP), `src/lib/wa-history.ts`,
  `src/app/api/wa-history/route.ts`, `src/components/wa-history-page.tsx`,
  `src/lib/wa-history.test.ts`.

---

## [2026-09-14] — דשבורד WA + TV WA: פיצול "זמן תגובה" ל"מוקד" ו"נציגה"

- **הבעיה (דיווח בעל החשבון, פנייה #74539):** "זמן תגובה ראשונה" (76 דק')
  נראה גבוה מדי. בדיקה מול `zendesk_ticket_transitions` גילתה שהבוט העביר
  את הפנייה לתור **הלא-משויך** ב-11:18, היא ישבה שם **70 דקות** בלי שיוך,
  ורק ב-12:28 שויכה בפועל לירין אללוף — שענתה כעבור 7 דקות. המספר הקיים
  (`handed_to_agent_at` → `first_agent_message_at`) מדד תמיד את ההמתנה
  הכוללת של הלקוח, כולל זמן בתור — לא את זמן התגובה של הנציגה עצמה.
- **הפתרון:** במקום להחליף את ההגדרה הקיימת, נוספה עמודה שנייה לצדה:
  - **"זמן תגובה מוקד"** — ההגדרה הקיימת, ללא שינוי.
  - **"זמן תגובה נציגה"** — מרגע שהפנייה שויכה בפועל לנציגה
    (`assigned_to_agent_at`, מה-transition האחרון של שיוך אמיתי לפני
    ההודעה הראשונה) ועד ההודעה שלה.
  - שני הממוצעים מוצגים **זה לצד זה, לא ממוזגים** — בעל החשבון ביקש לראות
    את שניהם ולהחליט בעצמו איזה מהם יזין שכר/בונוסים בהמשך.
- **נבדק מול דאטה חי:** פנייה 74539 — "מוקד" 76:30, "נציגה" 6:50 (תואם
  לחלוטין). מסך "דשבורד WA": ממוצע מוקד 01:27:39 מול ממוצע נציגה 26:31
  (הפרש גדול כצפוי, כי יש הרבה פניות backlog שישבו זמן רב בלי שיוך). נבדק
  גם במסך TV WA (אותה תשתית `summarizeByDepartment`).
- קבצים: `supabase/migrations/20260914130000_zendesk_assigned_to_agent_at.sql`
  (מרחיב את `recompute_ticket_transitions` הקיימת, לא job חדש — מתעדכן
  אוטומטית בכל סנכרון, בלי שינוי ב-Edge Function), `src/lib/wa-dashboard.ts`,
  `src/app/api/wa-dashboard/route.ts`, `src/components/wa-dashboard-page.tsx`,
  `src/components/wa-wallboard-client.tsx`, `src/lib/wa-dashboard.test.ts`.
  **לא נוגע ב"ביצועי WA"** (`recompute_wa_agent_daily`/`wa-history`) — רק
  שני המסכים החיים, לפי בקשת בעל החשבון.

---

## [2026-09-14] — "ביצועי WA": אותו תיקון "נענו/נסגרו לפי מתי זה קרה" גם ברשומה השמורה, רק קדימה

- המשך ישיר לתיקון הקודם היום (דשבורד/TV WA למטה) — עכשיו גם `recompute_wa_agent_daily`
  (ה-SQL מאחורי "ביצועי WA") סופר "נענו"/"נסגרו" לפי מתי זה קרה בפועל,
  לא לפי יום פתיחת הפנייה, בדיוק כמו דשבורד/TV WA.
- **בעיה שהתגלתה לפני התיקון:** יש job לילי (`wa-agent-daily-rollup-nightly`,
  00:30) שמריץ מחדש את `recompute_wa_agent_daily` על 31 הימים האחרונים
  **כל לילה**, אוטומטית — כלומר שינוי סתמי בפונקציה היה משנה בשקט מספרים
  היסטוריים כבר בלילה הקרוב.
- **הפתרון (אושר עם בעל החשבון — "רק קדימה, בלי אחורה"):** תאריך חיתוך
  קבוע בפונקציה, **2026-09-14**. ימים לפני התאריך הזה ממשיכים להיספר
  **לצמיתות** בהגדרה הישנה (נענה/נסגר משויך ליום שבו הפנייה *נפתחה*,
  בדיוק כמו היום), גם כשה-job הלילי ירוץ עליהם שוב. רק מ-14.09 ואילך
  נספר לפי מתי זה קרה בפועל.
- **נבדק:** הרצתי מחדש `recompute_wa_agent_daily` על 13.09 (לפני החיתוך)
  והתוצאה זהה ביט-לביט לפני/אחרי. על 14.09 (אחרי החיתוך) — יהב נסימי
  עברה מ"לא מופיעה בכלל" ל-`ticket_count=0, closed_count=7,
  responded_count=3`, תואם למה שכבר ראינו ב-TV.
- קובץ: `supabase/migrations/20260914120000_wa_agent_daily_activity_day.sql`
  (הוחל ישירות ב-Supabase דרך MCP, לא דרך git push).

---

## [2026-09-14] — דשבורד/TV WA: "נענו היום"/"נסגרו היום" סופרים לפי היום שבו זה קרה, לא לפי יום פתיחת הפנייה

- **הבעיה (דיווח: בורר "נציגות בחישוב" ב-TV WA הראה רק 3/3 נציגות):** "נענו היום",
  "נסגרו היום" ובורר הנציגות נבנו רק מתוך פניות **שנפתחו** היום. נציגה שענתה
  היום לראשונה על פנייה מאתמול, או סגרה היום פנייה ישנה — לא נכללה בכלל,
  וגם לא הופיעה בבורר. בדיקה מול 14.09 בפועל: 8+ נציגות עם פעילות אמיתית
  היום (יהב נסימי סגרה 6 פניות ישנות, מור אלקיים ו-3 נוספות ענו לראשונה
  על פניות מ-08.09–13.09) לא נספרו בשום מקום.
- **התיקון (אושר עם בעל החשבון, כולל הדוגמה של שעון עסקי: פנייה מאתמול
  אחרי שעות + מענה היום ב-08:05 = 5 דק' תגובה ראשונית היום, לא "בלתי
  נראה" כי הפנייה בת יום):** "נענו היום"/"נסגרו היום" (ומכאן גם מי
  מופיעה בבורר) סופרים עכשיו לפי **מתי בפועל קרה המענה/הסגירה**
  (`first_agent_message_at` / `solved_at`), לא לפי יום פתיחת הפנייה.
  "פניות היום" (ticketCount) **נשאר** כמו שהיה — רק פניות שנפתחו היום.
- **טווח בזמנו:** דשבורד WA + TV WA בלבד (`/api/wa-dashboard`); "ביצועי WA"
  (`recompute_wa_agent_daily`) עדיין ספר לפי יום הפתיחה. **הפער נסגר
  באותו יום** — ראה הרשומה שמעל.
- נבדק מול נתוני אמת (משתמש "שירות בדיקות", TV WA): הבורר עלה מ-3/3
  ל-9/9, ו"נפתרו היום" של יהב נסימי הראה 6 (במקום 0).
- קבצים: `src/lib/wa-dashboard.ts` (`summarizeTickets`/`summarizeByAgent`
  מקבלים `WaExtraActivity`), `src/app/api/wa-dashboard/route.ts` (שתי
  שאילתות חדשות: פניות ישנות שנענו/נסגרו היום), `src/components/wa-wallboard-client.tsx`,
  `src/components/wa-dashboard-page.tsx`.

---

## [2026-09-10] — "ביצועי WA": בחירת תקופת השוואה ידנית

- ליד "לעומת X–Y" יש עכשיו קישור **"בחירת תקופת השוואה"**: פותח שני
  שדות תאריך (מ/עד) לבחירת כל תקופה שרוצים להשוות מולה — לא רק התקופה
  האוטומטית הצמודה (באותו אורך, מיד לפני הטווח הנוכחי). "איפוס לתקופה
  האוטומטית" חוזר להתנהגות המקורית. הבחירה נשמרת ב-URL
  (`compareFrom`/`compareTo`) ומחזיקה מעמד ברענון/שיתוף קישור.
- ה-API (`/api/wa-history`) שולף עכשיו את שתי התקופות בשתי שאילתות
  נפרדות (לא טווח אחד רציף שמפוצל) כדי לתמוך בתקופת השוואה שאינה צמודה
  לטווח הנוכחי כלל.
- נבדק ידנית מול נתוני אמת (משתמש "שירות בדיקות"): השוואה מול 01.08–05.08
  לעומת ברירת המחדל, ואיפוס חזרה למספרים המקוריים.
- **תיקון המשך (אותו יום, דיווח על בלבול):** השוואת יום נוכחי בודד מול
  תקופה ידנית של יומיים (למשל 09.09–10.09 מול "היום" בלבד) הניבה מספר
  שנראה שרירותי (סכום שני הימים) בלי שום סימן שמדובר בטווח ארוך יותר —
  ול-10.09 שהיה גם בתקופה הנוכחית וגם בתקופת ההשוואה, מה שספר אותו
  פעמיים בפועל. לא נחסם (בכוונה — משווים יום מול יום זה שימוש לגיטימי),
  אלא הוצג לצד "לעומת X–Y" מספר הימים של כל תקופה ("יומיים" /
  "התקופה הנוכחית: יום אחד") כדי שאי-ההתאמה תהיה גלויה מיד.
- קבצים: `src/app/api/wa-history/route.ts`, `src/components/wa-history-page.tsx`.

---

## [2026-09-10] — "ממתין לתגובה" מתעלם מפניות בתזכורת/תיאום

- **הבעיה (דיווח על פנייה #69875):** פנייה ש"נציגה קבעה לה תזכורת"
  בזנדסק (או העבירה לתיאום לקוח / משלוח) נשארת מבחינת השדה הבסיסי
  `status` בערך `open` — זנדסק מסמן את זה רק ב-custom status נפרד
  ("תזכורת", "תאום לקוח" וכו', כולם תחת הקטגוריה "open"). המערכת הכירה
  רק את ה-status הבסיסי, ולכן ספרה גם את אלה כ"לקוח ממתין לתגובה",
  למרות שהנציגה כבר טיפלה וקבעה יוזמה משלה.
- **התיקון (אושר עם בעל החשבון):** רק custom status ברירת המחדל של
  "open" (**"פתוחה"**) נחשב "ממתין לתגובה"; כל שאר ה-custom statuses
  (תזכורת, תזכורת משלוח, ממתינה לתאום, התחל תאום לקוח, תאום לקוח,
  יום לפני משלוח, מתבצע) לא נספרים כהמתנה, גם אם ה-status הבסיסי הוא
  open. משפיע על "ממתינים לתגובה" בדשבורד WA וב-TV wallboard (מסכים
  שמשפיעים על שכר ובונוסים).
- **מנגנון:** טבלה חדשה `zendesk_custom_statuses` (id, קטגוריה, תווית,
  is_default) מסונכרנת מ-`/custom_statuses` של זנדסק לצד sync
  הרגיל; עמודה חדשה `zendesk_tickets.custom_status_id`. פנייה בלי
  custom_status_id מסונכרן (טרם עודכנה מאז הפריסה) נחשבת כברירת מחדל,
  כדי לא לאבד את ההמתנה שהייתה מוצגת לפני התיקון.
- קבצים: `supabase/migrations/20260910140000_zendesk_custom_statuses.sql`,
  `supabase/functions/sync-zendesk-tickets/index.ts` (`syncCustomStatuses`,
  `custom_status_id` בכל פנייה), `src/app/api/wa-dashboard/route.ts`
  (`mapTicketRow` מקבל את ברירת המחדל הפתוחה ומסנן לפיה).

---

## [2026-09-10] — תיעוד מעבר בין מחשבים (handoff)

- `CLAUDE.md` בשורש: נטען אוטומטית ב-Claude Code, מפנה לכל שאר התיעוד
  ומרכז את הכללים שכבר נשרפנו בהם (push = deploy, מיגרציות/פונקציות
  דרך MCP, PostgREST embeds, עמוד חדש = שלושה מקומות, מצב הדגמה, מדדי
  שכר).
- `HANDOFF.md`: חשבונות וחיבורים, הקמה מקומית, טבלת **כל הסודות** ואיפה
  כל אחד חי ומי קורא אותו, פריסה, אימות פריסה בלי hPanel, קבצים שקיימים
  רק מקומית, פריטים פתוחים.
- `HANDOFF_SECRETS.local.md` (git-ignored, עובר רק ב-ZIP): ערכי הסודות
  שניתן היה לקרוא (Vault, .env.local) ושמות אלה שלא (סודות Edge Functions).
- `docs/agent-memory/`: העתק של הערות הזיכרון של Claude Code מהמחשב
  הראשון, כדי שמחשב חדש יקבל אותן.

---

## [2026-09-10] — TV WA: "נפתרו היום" על כרטיס הזמינות של כל נציגה

- בכרטיס של כל נציגה ב"זמינות נציגות" (TV בלבד) מופיע כמה פניות היא
  פתרה היום — אותו מספר כמו "נסגרו היום" בטבלת הנציגות בדשבורד WA
  (זיכוי למי שהייתה משויכת ברגע הפתרון). מכבד את בורר הנציגות.
- קובץ: `src/components/wa-wallboard-client.tsx`.

---

## [2026-09-10] — מסך "תפקידי נציגות" בהגדרות

- **הגדרות → תפקידי נציגות**: הוספת תפקיד (שם ביחיד לבחירה + כותרת
  הקבוצה ברבים), שינוי שם, סדר (חצים למעלה/למטה) ומחיקה. הסדר הוא סדר
  הקבוצות ב"זמינות נציגות". "נציגת מענה" הוא ברירת המחדל של כל נציגה
  חדשה ולא נמחק; תפקיד עם נציגות לא נמחק עד שמעבירים אותן.
- הרשימה כבר לא קבועה בקוד: טבלה `agent_roles` + FK מ-`agents.role`
  (במקום ה-CHECK). ה-FK הופך את `agents` ל"טבלת קישור" בעיני PostgREST
  בין מחלקות לתפקידים — אין embed כזה, וכל ה-embeds הקיימים נבדקו (200).
- `/api/settings/agent-roles` (GET לכל מחובר; POST/PATCH/DELETE למנהל
  מערכת), `/api/wa-dashboard` מחזיר `roles`, hook `useAgentRoles`.
- מיגרציה: `supabase/migrations/20260910120000_agent_roles_table.sql`.
  קבצים: `src/components/agent-roles-settings.tsx`, `src/lib/agent-roles.ts`,
  `src/hooks/use-agent-roles.ts`, `src/components/settings-tabs.tsx`.

---

## [2026-09-10] — תפקיד לכל נציגה; "זמינות נציגות" מקובץ לפי תפקיד

### מוצר
- **תפקיד לכל נציגה**, נבחר מרשימה בעמוד "נציגים וצוותים" (מנהל מערכת
  בלבד; שאר המשתמשים רואים את התפקיד כטקסט): נציגת מענה (ברירת מחדל),
  נציגת סניפים, שימור לקוחות, אחמ"שית, מתאמת, מנהלת, לא עובד/ת. שינוי
  נשמר מיד ותופס בכל המסכים.
- **"זמינות נציגות"** (TV + דשבורד WA) מחולק לקבוצות לפי התפקיד, בסדר
  הזה, נציגות מענה ראשונות ו"לא עובדות" אחרונות; קבוצה ריקה לא מוצגת.
  המספרים בכותרת ("מקוונות / מקומות פנויים") סופרים **רק נציגות מענה**;
  לכל קבוצה יש ספירה משלה.
- **שום מדד אחר לא מושפע**: פניות, ממתינים לתגובה, טבלת הנציגות ו"ביצועי WA"
  נשארים אצל מי שטיפלה בפועל, גם אם היא מנהלת או אחמ"שית.
- שיוך ראשוני: לירן רבני – מנהלת; שני דנוך, אורטל צפניה – אחמ"שיות;
  נוי עזגד, קורל ויזמן – מתאמות.

### תשתית
- `agents.role` (CHECK constraint, לא טבלה נפרדת — כדי לא להוסיף FK
  ש-PostgREST יפרש כטבלת קישור); מדיניות "admins update agents".
  סנכרון Aircall לא נוגע בעמודה. מיגרציה:
  `supabase/migrations/20260910100000_agent_roles.sql`.
- `PATCH /api/agents` (`{agentId, role}`, מנהל מערכת). `/api/dashboard`
  ו-`/api/wa-dashboard` (זמינות) מחזירים `role`.
- קבצים: `src/lib/agent-roles.ts` (המילון), `src/lib/wa-dashboard.ts`
  (`groupAvailabilityByRole`, `answeringAgents` + טסטים),
  `src/components/section-pages.tsx` (`AgentRoleField`), שני הקומפוננטים.

---

## [2026-09-10] — דשבורדי WA: שני תורים, ממתינים לתגובה מכל הימים

### מוצר (דשבורד WA + דשבורד TV WA)
- **התור פוצל לשתי תיבות קבועות** במקום כותרת שמתחלפת לפי השעה:
  **"ממתינים לשיוך נציגה"** (הבוט העביר בתוך שעות הפעילות) ו**"ממתינים
  אחרי שעות הפעילות"** (הבוט העביר מחוץ להן; ייענו עם פתיחת המשמרת).
  החלוקה לפי רגע ההעברה של הבוט, לא לפי השעה הנוכחית. שתיהן לפי מחלקה,
  זמן אמת, לא מושפעות מבורר הנציגות.
- **"ממתינים לתגובה" מכסה את כל הפניות הפתוחות אצל הנציגות מ-08.09 ואילך**,
  לא רק של היום. פנייה מיום קודם מסומנת "נפתחה ד׳ 09.09". 08.09 הוא היום
  הראשון עם מעקב מלא אחרי "מי כתב אחרון" והעברת הבוט — לפני כן אין אות
  אמין ל"עדיין לא נענה", ולכן פניות ישנות יותר לא נכנסות בכלל. הריבועים
  (3/7/10, פניות היום) והטבלה לפי נציגה נשארים על היום בלבד.
- קבצים: `src/app/api/wa-dashboard/route.ts` (`openBacklog`,
  `BACKLOG_FROM_DATE`, `mapTicketRow`), `src/lib/wa-dashboard.ts`
  (`splitQueueByBusinessHours`), `src/components/wa-wallboard-client.tsx`
  (`QueueBox`), `src/components/wa-dashboard-page.tsx` (`QueueSection`).

---

## [2026-09-09] — רשומה יומית לכל נציגה ועמוד "ביצועי WA"

### מוצר
- **עמוד חדש "ביצועי WA"** (`/wa-dashboard/history`, מתחת לדשבורד WA
  בתפריט; אדמין בלבד עד שמעניקים הרשאה ב"ניהול משתמשים"): טווח (היום /
  השבוע / שבוע שעבר / החודש / 30 יום / תאריכים), 5 מדדים לתקופה עם השוואה
  לתקופה הקודמת באותו אורך, טבלה לפי יום, טבלה לפי נציגה עם פירוט יומי
  בלחיצה, ייצוא CSV לאקסל, טאב לכל מחלקה.
- **השכר והבונוסים נשענים על המספרים האלה**, ולכן הם נשמרים במסד ולא
  מחושבים מחדש מפניות חיות בכל צפייה.

### תשתית
- טבלה `wa_agent_daily`: שורה לנציגה × יום × מחלקה (לפי יום פתיחת
  הפנייה): פניות, פתוחות, נסגרו, נענו, **סכומי** זמני תגובה ראשונה וסגירה
  (סכומים ולא ממוצעים, כדי שכל טווח יסתכם במדויק), נענו תוך פחות מ-3 /
  מעל 3 / 7 / 10 דק'. אותן הגדרות בדיוק כמו בדשבורד WA.
- `recompute_wa_agent_daily(day)` בונה יום מחדש; pg_cron
  `wa-agent-daily-rollup` (היום + אתמול כל 10 דקות) ו-
  `wa-agent-daily-rollup-nightly` (31 יום אחורה ב-00:30 UTC, לפניות שנסגרו
  או הועברו ימים אחרי). מולא לאחור מ-01.08.2026.
- `business_seconds(from, to, schedule)` — השעון העסקי ב-SQL, שורה-לשורה
  כמו `src/lib/business-clock.ts`; 10 מקרי הבדיקה של TypeScript הורצו מולו
  ותאמו. טבלת `israel_holidays` (2026–2030) נוצרת מ-
  `src/lib/israel-holidays.ts` — לרענן לפני 2031.
- `admin-users` v14: העמוד ברשימת העמודים המותרים.
- מיגרציה: `supabase/migrations/20260909300000_wa_agent_daily.sql`.
  קבצים: `src/lib/wa-history.ts` (+ טסטים), `src/app/api/wa-history/route.ts`,
  `src/components/wa-history-page.tsx`, `src/app/wa-dashboard/history/page.tsx`.

### תקלה בפרודקשן ותיקונה (כ-15 דקות)
- אחרי המיגרציה **כל** שאילתה עם `agents(... departments(...))` החזירה
  300 / `PGRST201` והדשבורדים (WA, ניטור, מעקב פניות, זמני סטטוס) קיבלו 500:
  PostgREST זיהה את `wa_agent_daily` (FK לנציגות + FK למחלקות) כטבלת
  קישור נוספת בין נציגות למחלקות. תוקן ב-`departments!department_id(...)`
  בשבע שאילתות. **כלל קבוע:** לפני כל מיגרציה שמוסיפה טבלה עם FK לשתי
  טבלאות קיימות — לרמוז את עמודת ה-FK בכל embed ביניהן (ראה
  `PROJECT_CONTEXT.md`).

---

## [2026-09-09] — שעון עסקי: זמנים רק בשעות הפעילות, ללא שישי, חגים וערבי חג

### מוצר
- **כל מדדי הזמן בדשבורדי WA** (תגובה ראשונה, ריבועי 3/7/10, "פחות מ-3",
  ממתינים לתגובה בלייב, זמן עד סגירה, הטבלה לפי נציגה) רצים על שעון
  שעובד רק בשעות הפעילות של המחלקה (הגדרות → שעות פעילות: ראשון–חמישי
  08:00–15:00) ועוצר מחוצה להן. לקוח מ-14:58 שנענה למחרת ב-08:03 = 5 דקות;
  לקוח מ-20:00 שנענה ב-08:10 = 10 דקות; חמישי 14:50 → ראשון 08:15 = 15 דקות.
- **חגי ישראל וערבי חג** מחושבים מהלוח העברי (Intl, ללא ספרייה): ראש השנה
  (יומיים), יום כיפור, סוכות, שמחת תורה, פסח, שביעי של פסח, שבועות — כל
  אחד עם ערבו — ויום העצמאות (בלי ערב, עם ההזזות הרשמיות). חול המועד,
  פורים וערב יום הזיכרון הם ימי עבודה.
- **התור לשיוך נציגה נשאר על זמן אמת** (שם רוצים פעולה מיידית). השעון
  משתמש בשעות המחלקה גם כשמתג "ניתוב מחוץ לשעות" של השיחות כבוי.
- שורת הסבר בשני המסכים: "כל הזמנים נמדדים בשעות הפעילות בלבד (…)".
- קבצים: `src/lib/business-clock.ts` (+ טסטים), `src/lib/israel-holidays.ts`,
  `src/lib/wa-dashboard.ts` (`currentlyWaiting` / `firstResponseElapsed`
  מקבלים `clock`), `src/app/api/wa-dashboard/route.ts` (`businessHours`
  ב-payload), שני הקומפוננטים.
- מאוחר יותר באותו יום: כותרת התור התחלפה ל"ממתינים אחרי שעות הפעילות"
  מחוץ לשעות (`businessOpenAt`) — הוחלף ב-10.09 בשתי תיבות קבועות.

---

## [2026-09-09] — TV WA: בורר נציגות, סדר חדש, רק סטטוס "פתוחה"

- **"ממתינים לתגובה" סופר רק פניות בסטטוס "פתוחה"**: "בהמתנה", "מושהה",
  "חדשה" (בתור), "פתורה" ו"סגורה" לא נכנסות. קודם נכנסו גם "בהמתנה".
- **סדר ה-TV:** ריבועי המענה → ממתינים לשיוך נציגה → ממתינים לתגובה →
  זמינות נציגות → תיבות המחלקה.
- **בורר נציגות גם ב-TV** (כפתור "נציגות X/Y" בכותרת, כתום כשמסונן); כל
  המספרים במסך מחושבים לפי הבחירה. הבחירה נשמרת בדפדפן לפי מחלקה
  ומשותפת עם דשבורד WA (`src/lib/wa-agent-filter.ts`).
- **לקוחות בתהליך הבוט לא ניתנים לספירה**: Zendesk יוצר את הפנייה רק ברגע
  ההעברה לנציג (ב-150 פניות של 09.09 ההעברה נרשמה תוך שנייה מהיצירה).
  הדרך היחידה היא Sunshine Conversations API עם מפתח נפרד.

---

## [2026-09-09] — זמינות נציגות חיה מ-Zendesk; TV מציג את כל הממתינים

- **"זמינות נציגות"** בשני המסכים: סטטוס חי מ-Zendesk Agent Availability API
  (מקוון / לא פעיל / העברה בלבד / לא מקוון / סטטוס מותאם כמו "הפסקה"),
  עומס Messaging X/7 ו"פנויה לעוד N". מתרענן כל דקה עם סנכרון הפניות.
  ניתן לחרוג מהקיבולת (9/7) דרך העברות/לקיחה ידנית.
- טבלה `zendesk_agent_availability` (מיפוי לנציגה לפי אימייל);
  `sync-zendesk-tickets` v10 עם `syncAgentAvailability`.
- ה-TV מציג **את כל** הלקוחות הממתינים, מקובצים לפי נציגה, בלי מגבלה.
- תיקון: `agents!agent_id` בכל embed של `zendesk_tickets` אחרי הוספת
  ה-FK של `first_response_agent_id` / `solved_by_agent_id` (אותו PGRST201).
- מיגרציה: `20260909230000_zendesk_agent_availability.sql`.

---

## [2026-09-09] — סגירה מדויקת וזיכוי לנציגה שהייתה משויכת באותו רגע

- `zendesk_ticket_transitions` (מעברי סטטוס ושיוך מייצוא האירועים),
  `solved_at` (רגע ה"פתורה" המדויק, לא העדכון האחרון),
  `first_response_agent_id` / `solved_by_agent_id` — התגובה הראשונה
  והסגירה נזקפות למי שהייתה משויכת **באותו רגע**, גם אם הפנייה עברה
  אחר כך. "פניות היום" נזקפות לנציגה המשויכת כרגע.
- טבלת הגדרות מלאה של כל מספר במסך נמסרה למנהל (ראה
  `PROJECT_CONTEXT.md` → WhatsApp dashboards).
- מיגרציה: `20260909220000_zendesk_ticket_transitions.sql`.

---

## [2026-09-06 → 2026-09-08] — דשבורד WA ודשבורד TV WA (Zendesk Messaging / וואטסאפ)

### מוצר
- שני עמודים חדשים: **"דשבורד WA"** (`/wa-dashboard`, בורר תאריך, טאב לכל
  מחלקה) ו**"דשבורד TV WA"** (`/wa-dashboard/tv?department=…`, מסך קיר,
  היום בלבד). אדמין בלבד עד הענקת הרשאה; `admin-users` תוקן כך שכל
  העמודים מופיעים ב"עמודים מותרים" (חסרו 5).
- **ממתינים לתגובה כרגע** בראש המסך: לקוחות שההודעה האחרונה בשיחה היא
  שלהם, מדורגים 3/7/10 דק', עם הנציגה המשויכת; ב-TV גם גיל הפנייה הכולל.
- **תגובה ראשונה**: מרגע שהבוט העביר לנציגות ועד ההודעה הראשונה של נציגה
  אנושית (לא בוט). ריבועים: פחות מ-3 (ירוק), מעל 3/7/10; ממוצע; "X נענו
  מתוך Y". פתוחות ונסגרו בנפרד; זמן עד "פתורה"/"סגורה".
- **תור "ממתינים לשיוך נציגה"**: סטטוס "חדשה" בלי נציגה, לפי מחלקה
  (`zendesk_group_departments`); ישנות מיממה נספרות ולא מוצגות.
- **בורר נציגות** (החרגה מכל המספרים), טבלה לפי נציגה עם עמודות בשם
  (פניות היום / נסגרו היום / עדיין פתוחות / ממתינים כרגע / תגובה ראשונה
  ממוצעת / זמן סגירה ממוצע) ופירוט פניות בלחיצה.
- צבעי TV רגועים (הבעלים ביקש פחות אדום).

### איך המידע נאסף (חשוב, לא ברור מאליו)
- ב-Zendesk Messaging הודעות **אינן** תגובות בפנייה בזמן אמת — כל השיחה
  נוחתת כ-`chat_transcript` אחד מאוחר יותר. לכן "מי כתב אחרון" נלקח
  מ**טריגר Messaging שמחליף תגיות** (`last_whatsapp_reply_agent` /
  `last_whatsapp_reply_customer`) בכל הודעה; הבוט לא מפעיל אותו. ייצוא
  האירועים המצטבר (`ticket_events`) מספק את ההחלפות עם `added_tags`.
- **העברת הבוט** = מעבר סטטוס `open → new` (הבוט מנקה את השיוך וקובע
  קבוצה). `OfferedToEvent` **אינו** ההעברה (נורה רק כשהניתוב מציע לנציגה
  פנויה, לפעמים דקות אחרי — פנייה 71209 ישבה 6.5 דק' בלי להיראות).
- הסנכרון ממשיך לעדכן פניות מוכרות גם מעבר לחלון החודש (פניות מאוגוסט
  קפאו כ"חדשה"). פניות בסטטוס `deleted` מוחרגות.
- Edge `zendesk-probe` (v7, פרוס בלבד, `x-sync-secret`): אודיטים של
  פנייה, אירועים מ-timestamp, GET ברשימת היתר — לאבחון.
- מיגרציות: `20260906090000` … `20260909200000` (dashboard, response &
  close time, whatsapp_messages, customer_waiting_since, handoff,
  group_departments, handoff_is_status_new).
- קבצים: `src/lib/wa-dashboard.ts` (+ טסטים), `src/app/api/wa-dashboard/route.ts`,
  `src/components/wa-dashboard-page.tsx`, `src/components/wa-wallboard-client.tsx`,
  `src/app/wa-dashboard/{page,tv/page}.tsx`, `src/lib/app-pages.ts`,
  `src/components/sidebar.tsx`, `supabase/functions/sync-zendesk-tickets`.

---

## [2026-08-10] — אחוז המענה בטבלת ההמתנה יושר לדונאט

### הבאג
- על אותו מסך הופיעו שני מספרים בשם "אחוז מענה": הדונאט הראה **81%**
  והטבלה **77.3% / 75.9%**, בלי שום רמז מאיפה הפער.
- שניהם היו נכונים אריתמטית אבל חילקו במכנה אחר. הדונאט מחשב
  `נענו / (נענו + לא נענו)` כאשר "לא נענו" **אינו כולל ניתוקים מהירים** —
  לקוחות שניתקו תוך הסף המוגדר (10 שניות אצלכם). הטבלה חילקה בכל
  השיחות הנכנסות.
- בפועל: `533/662 = 80.5%` מול `533/697 = 76.5%`. ההפרש הוא **35 ניתוקים
  מהירים**.

### התיקון
- `waitStatsByDepartment` מקבלת את סף הניתוק המהיר ומחסירה אותם מהמכנה,
  בדיוק כמו `calculateKpis`. הדונאט צודק: מי שניתק אחרי 3 שניות לא נכשל
  באשמת המוקד.
- **שיחות נכנסות נשאר על כל השיחות** (697), כדי שימשיך להתאים לאריח
  שמעליו. רק אחוז המענה השתנה.
- אחוזי ההמתנה לא נגעו: ניתוק מהיר באמת לא חיכה מעל דקה, ולכן מקומו
  במכנה שם נכון.
- נוסף הסבר על המסך עצמו, כדי שלא יצטרכו לשאול שוב.
- טסט שמצליב את שני המדדים ומוודא שהם לא יכולים להתפצל שוב (סה״כ 50).
- קבצים: `src/lib/metrics.ts`, `src/lib/metrics.test.ts`,
  `src/components/dashboard-client.tsx`.

---

## [2026-08-09] — זמן המתנה פר מחלקה בעמוד "ניטור בזמן אמת"

### מוצר
- טבלת השוואה חדשה מתחת לרצועת המדדים: **המדדים כשורות, המחלקות
  כעמודות**, כדי שאפשר יהיה להשוות ביניהן במבט אחד.
- השורות: שיחות נכנסות · **אחוז מענה** · נתח מזמן ההמתנה · המתנה ממוצעת
  (שנענו) · נענו תוך פחות מ-60 שניות · חיכו מעל 60 שניות.
- כל אחוז צבוע לפי רמה, כך שהעין תופסת חריגה בלי לקרוא מספרים.
- הנתונים מכבדים את כל הפילטרים של העמוד — טווח תאריכים, מחלקה, נציג
  וחיפוש — ולא רק את היום הנוכחי.

### הגדרות המדדים
- **אחוז מענה** ו**חיכו מעל היעד** נמדדים מול **כל** השיחות הנכנסות: לקוח
  שהתייאש אחרי ארבע דקות חיכה בדיוק כמו מי שנענה בסוף.
- **נענו תוך פחות מ-60 שניות** נמדד מול **שיחות שנענו בלבד** — שיחה שאיש
  לא ענה לה חיכתה הרבה, אבל אף אחד לא איחר לענות לה.
- **נתח מזמן ההמתנה** מראה איפה הכאב מרוכז, אבל נגרר אחרי נפח. **נענו תוך
  דקה** הוא מדד ביצועים שאינו תלוי בנפח — במדידה של 09/08: אספקות 19.7%
  מול שירות לקוחות 44.1%, פער שנעלם לגמרי בממוצע מאוחד.
- האחוזים בעשירית אחת ולא בשלם, כדי ש-72.4% ו-27.6% לא יקרסו ל-72% ו-28%.

### תשתית
- `waitStatsByDepartment` ב-`metrics.ts`, משתמשת ב-`inboundWaitSeconds`
  הקיימת כדי שהחישוב יזדהה עם שאר המערכת כולל נפילות לאחור.
- שיחות `in_progress` לא נספרות — אין להן עדיין זמן המתנה סופי, והן היו
  מושכות את הממוצע לכיוון מה שמצלצל באותו רגע.
- הגדרת השורות יושבת במקום אחד (`WAIT_ROWS`), כך שכל מחלקה מרונדרת מאותה
  הגדרה והעמודות לא יכולות להיפרד זו מזו.
- 9 טסטים חדשים (סה״כ 48).
- קבצים: `src/lib/metrics.ts`, `src/lib/metrics.test.ts`,
  `src/components/dashboard-client.tsx`.

---

## [2026-08-09] — מסך מוקד (TV): בוקסת זמן המתנה פר מחלקה

### מוצר
- נוספה שורת בוקסות מתחת לרצועת המדדים, **אחת לכל מחלקה**: כמה ממתינים
  כרגע, **ההמתנה הארוכה ביותר** ו**הממוצע** — מתקתקים בזמן אמת כל שנייה.
- צבע לפי חומרה: מעל 3 דקות אדום, מעל דקה כתום, אחרת טורקיז.
- בוקסה מוצגת גם כשאין ממתינים. על מסך קיר "0" יציב הוא מידע; בוקסה
  שנעלמת נראית כמו מסך שנתקע.
- ממתינים ללא שיוך מחלקה מקבלים בוקסה משלהם — קודם הם נספרו בפאנל
  שמתחת בלי שיהיה להם מקום למעלה.

### הפרדת המחלקות — לא נדרש מנגנון חדש
- כבר קיימים משתמשי TV ייעודיים: **"מחלקת אספקות"** ו**"מחלקת שירות
  לקוחות"**, שניהם `viewer` עם הרשאה ל-`wallboard` בלבד ועם מחלקה מוגדרת.
- `/api/dashboard` מצמצם לפי המחלקה של הצופה (`getDepartmentScope`), ולכן
  מסך שמחובר עם אחד מהם מקבל **רק** את השיחות והמחלקות שלו ורואה בוקסה
  אחת. אין ערבוב, ואין מה להגדיר.
- אדמין או מנהל רואה את שתי המחלקות **זו לצד זו כשתי בוקסות נפרדות**,
  ולא מספר אחד מעורבב — זה החלק שהיה מטעה.

### פרט מימוש
- המספרים מחושבים בזמן הרינדור ולא ב-`useMemo`, כדי שיעקבו אחרי שעון
  השנייה של המסך; memo על רשימת השיחות היה מקפיא אותם עד שהייתה נכנסת או
  מסתיימת שיחה.
- קבצים: `src/components/wallboard-client.tsx`.

---

## [2026-08-09] — "פניות פתוחות": רק פניות שנסגרו, בלי WhatsApp

### מוצר
- העמוד מציג כעת **רק פניות שנסגרו** ולא תועדו, במקום כל סטטוס.
- **פניות WhatsApp הוצאו** — 968 פניות במערכת. הן נפתחות מהודעה של הלקוח
  ולא מטיפול של נציגה בשיחה, ולכן לא שייכות למדד הזה.
- הטקסטים בעמוד עודכנו בהתאם.

### הגדרת "סגורה"
- ב-Zendesk של הלקוח מוצגים ארבעה סטטוסים: פתוחה / תזכורת / בהמתנה /
  **פתורה**. "פתורה" היא `solved` — מה שנציגה מסמנת כשסיימה.
- הסינון הוא `solved` **וגם** `closed`. הסיבה: `closed` הוא בדיוק אותה
  פנייה אחרי ארכוב אוטומטי של Zendesk כמה ימים אחר כך, והוא לא מוצג
  בממשק שלהם כלל. סינון ל-`solved` בלבד היה גורם לפנייה לא-מתועדת
  **להיעלם מעצמה** מתאריך ישן ברגע שהתיישנה.
- בפועל היום: `closed` מילולית = פנייה אחת; `solved`+`closed` = 215.

### תשתית
- עמודה חדשה `via_channel` על `zendesk_tickets`, מאונדקסת יחד עם סטטוס
  ותאריך. הוצאה מ-`raw` כי בדיקת `raw->'via'->>'channel'` בכל בקשה קוראת
  את כל ה-JSONB לכל שורה בלי אינדקס — בטבלה שגדלה ב-~1,300 שורות ביום,
  ובזמן שה-DB כבר בלחץ CPU.
- פונקציה חדשה `zendesk_undocumented_summary` עם הכללים מוטמעים.
- נקודת קצה ייעודית `/api/open-tickets` במקום לעמס פרמטרים על הקיימת.
- הסנכרון מאכלס `via_channel` לפניות חדשות; 17 שורות שנכנסו בין הבקפיל
  לפריסה מולאו ידנית.
- קבצים: `supabase/migrations/20260809110000_zendesk_via_channel_and_undocumented_summary.sql`,
  `supabase/functions/sync-zendesk-tickets/index.ts`,
  `src/app/api/open-tickets/route.ts`, `src/lib/tickets.ts`,
  `src/components/open-tickets-page.tsx`.

---

## [2026-08-09] — תיעוד מאוחר מתעדכן גם בדרופ-דאון הפתוח

### הבעיה
- נציגה שמוסיפה הערה שעה אחרי פתיחת הפנייה נקלטת נכון בנתונים — אומת:
  **1,082 פניות תועדו באיחור** (מ-5 דקות ועד 7 ימים), 88 מהן בשעתיים
  האחרונות. לדוגמה פנייה 28056 נפתחה ב-09:19 ותועדה ב-10:50.
- אבל בעמוד "פניות פתוחות" רשימת הפניות שנפתחת בדרופ-דאון נטענה **פעם
  אחת ונשמרה במטמון**. עם דרופ-דאון פתוח, המונה בכותרת ירד מ-21 ל-20
  בעוד שהפנייה שתועדה נשארה מוצגת ברשימה.

### התיקון
- אפקט הרענון מרענן עכשיו גם את הנציגה הפתוחה, לא רק את הספירות.
- הטעינה בלחיצה הוסרה — הצבת `expanded` מפעילה את האפקט ממילא, ושתי
  הקריאות יחד היו כפילות.
- ללא ספינר ברענון: הוא מוצג רק כשאין עדיין נתונים.
- "מעקב פניות" כבר טיפל בזה נכון — הוא מושך מחדש הכול כל 20 שניות,
  כולל עם פילטר "לא תועדו", כך שפנייה שתועדה נושרת מהרשימה מעצמה.
- קבצים: `src/components/open-tickets-page.tsx`.

---

## [2026-08-09] — תת-עמוד "פניות פתוחות": מי לא תיעד היום

### מוצר
- תת-עמוד חדש בתפריט הצד תחת "מעקב פניות" (`/ticket-tracking/open`),
  מוצג מוזח כדי שיהיה ברור שהוא בן של העמוד שמעליו.
- **טבלה יומית** של הנציגות שהשאירו פניות ללא תיעוד, בכל סטטוס, ממוינת
  מהגרוע לפחות. ברירת המחדל היא היום הנוכחי, עם בורר תאריך.
- לחיצה על שם נציגה **פותחת דרופ-דאון** עם הפניות שלה: מספר הפנייה, שם
  הלקוח, טלפון הלקוח, תאריך ושעה בשעון ישראל, וסטטוס.
- שני כרטיסי סיכום: כמה פניות ללא תיעוד וכמה נציגות מעורבות.

### תשתית
- לא נדרשה נקודת קצה חדשה — העמוד משתמש ב-`/api/zendesk-tickets` הקיים:
  הספירות מגיעות מ-`summary` שכבר מחושב במסד, והפניות של נציגה נטענות
  **רק כשפותחים אותה**. יום עמוס עובר 1,300 פניות; שליחת כולן מראש כדי
  להציג כמה מספרים היא בדיוק מה שהאט את עמוד האב.
- `app-pages.ts` ו-`sidebar.tsx` תומכים כעת בפריט בן (`child`).
- קבצים: `src/components/open-tickets-page.tsx`,
  `src/app/ticket-tracking/open/page.tsx`, `src/lib/app-pages.ts`,
  `src/components/sidebar.tsx`.

---

## [2026-08-06] — מעקב תיעוד: מי כותב בפניות ומי לא

### מוצר
- בעמוד "מעקב פניות" נוספה עמודה **"תועדה"** לכל פנייה, ובטבלת הנציגות
  נוספו **"לא תועדו"** ו-**"אחוז תיעוד"** עם מד ויזואלי.
- **פילטר תיעוד** — "הכול / לא תועדו / תועדו". הסינון מתבצע בשאילתה,
  כך שבחירת "לא תועדו" מציגה את כל הפניות הרלוונטיות בטווח.
- כרטיסי הסיכום כוללים עכשיו "תועדו" ו"לא תועדו".

### למה לא השתמשנו במדד המובנה של Zendesk
- ל-Zendesk יש `replies` שנראה בדיוק כמו הנדרש, אבל הוא סופר תגובות
  **פומביות** של נציג. הנציגות כאן כותבות **הערות פנימיות** ("הוסף הערה")
  דרך אפליקציית Aircall, ולכן `replies` החזיר 0 כמעט תמיד — שימוש בו
  היה מסמן את כל המוקד כ"לא מתעד".
- מיפוי בפועל של הכותבים בשעתיים: **Aircall Bot 150** תגובות (סיכומי
  שיחה אוטומטיים), **מערכת Zendesk 102** (טריגרים), לקוחות ב-WhatsApp,
  ורק **~37 תגובות של נציגות אמיתיות**.
- ההגדרה שנבחרה: תגובה נחשבת תיעוד כשהכותב שלה הוא **הנציגה המשויכת
  לפנייה**. השוואת `author_id` ל-`assignee_id` — בלי צורך בזיהוי תפקידים.
- הערה פנימית ותשובה פומבית נספרות **בנפרד** אבל שתיהן נחשבות תיעוד:
  נציגה שענתה ללקוח באריכות בבירור טיפלה בפנייה.

### תשתית
- `zendesk_tickets` קיבלה `agent_note_count`, `agent_reply_count`,
  `last_agent_comment_at`, ועמודה מחושבת `documented` (generated) שלא
  יכולה לסטות מהמונים שמזינים אותה.
- טבלה חדשה `zendesk_ticket_comments` שומרת את **אירועי התגובות** ולא
  מונה מצטבר. הסיבה: הייצוא ההדרגתי של Zendesk יכול להחזיר אותו אירוע
  בגבול סמן, וכל backfill היה מנפח את הספירות בלי דרך לזהות. upsert לפי
  מזהה האירוע הוא idempotent, והמונים מחושבים מחדש ממה שנשמר.
- **גוף התגובות לא נשמר** — השאלה היא רק מי כתב ומתי; שמירת התוכן הייתה
  עותק של שיחות הלקוחות ללא תועלת.
- `sync-zendesk-tickets` מושכת `incremental/ticket_events` בסמן נפרד
  (6 עמודים לריצה, כדי לא לחרוג ממגבלת 10 בקשות לדקה), ומריצה
  `recompute_ticket_documentation` רק לפניות שנגעו בהן.
- הבקפיל מתחילת החודש מתנקז מעצמו דרך ה-cron שרץ כל דקה.
- קבצים: `supabase/migrations/20260806150000_zendesk_documentation_tracking.sql`,
  `supabase/functions/sync-zendesk-tickets/index.ts`, `src/lib/tickets.ts`,
  `src/app/api/zendesk-tickets/route.ts`,
  `src/components/ticket-tracking-page.tsx`.

---

## [2026-08-06] — "מעקב פניות": קיצור זמן התגובה מ-4 דקות ל-~80 שניות

### הבעיה
- פנייה שנפתחה ב-16:38:58 נקלטה רק ב-16:42:01 והופיעה על המסך בסביבות
  16:43 — עד ~4 דקות. הנתונים לא היו שגויים, רק איטיים: עד 2 דקות
  להמתנה ל-cron ועוד עד דקה לרענון העמוד.
- אי אפשר היה פשוט להאיץ את הטיימר: כל רענון משך את **כל** הפניות בטווח
  (8,000+ שורות, ~1.5MB, תשע שאילתות מדופדפות) רק כדי לספור אותן
  בדפדפן. להריץ את זה בתדירות גבוהה מכמה טאבים במשרד זה בדיוק נפח
  הבקשות פר-IP שהפיל את דפי השיחות ל-403.

### התיקון — קודם לייעל, אחר כך להאיץ
- `public.zendesk_ticket_summary(from, to)` מחשבת את הספירות פר נציגה
  **במסד**. הרענון מעביר כמה מאות שורות במקום את כל החודש.
- הרשימה מוגבלת ל-500 האחרונות, והסינון (נציגה/סטטוס) עבר **לשאילתה**
  ולא לרשימה כבר-חתוכה — כך שבחירת נציגה מציגה את כל הפניות שלה בטווח,
  ולא רק את אלה שנפלו בתוך 500 האחרונות הכלליות.
- הסנכרון מ-Zendesk עלה מכל 2 דקות ל**כל דקה**; העמוד מ-60 ל-**20 שניות**.
  זמן התגובה הגרוע ביותר ירד ל-~80 שניות.
- קבצים: `supabase/migrations/20260806143000_zendesk_ticket_summary_and_faster_sync.sql`,
  `src/app/api/zendesk-tickets/route.ts`, `src/lib/tickets.ts`,
  `src/components/ticket-tracking-page.tsx`.

---

## [2026-08-06] — עמוד חדש: "מעקב פניות" (Zendesk)

### מוצר
- עמוד חדש `/ticket-tracking`: כל פניות Zendesk מתחילת החודש, עם **שם
  הלקוח, מספר הטלפון שלו, מועד פתיחת הפנייה, הנציגה המטפלת והסטטוס**.
- טבלת מדדים פר נציגה — כמה פניות פתוחות וכמה סגורות. לחיצה על שורת
  נציגה מסננת את רשימת הפניות אליה.
- כרטיסי סיכום, סינון לפי פתוחות/סגורות, וטווח תאריכים.
- העמוד חי: מתרענן כל 60 שניות, והסנכרון מ-Zendesk רץ כל 2 דקות.

### רקע — האינטגרציה נבנתה מחדש
- Zendesk היה **מנותק**: `/api/settings/zendesk` מחזיר 410, לא הייתה טבלת
  הגדרות, ו-`zendesk_customers` הייתה ריקה. לא הייתה טבלת פניות כלל.
- החיבור החדש משתמש ב-Edge Function Secrets `mail_Zendesk` + `API_Zendesk`
  מול הסאבדומיין `rcity`. אומת לפני שנבנה משהו: מזדהה כאדמין, 27,091
  פניות בחשבון, 48 נציגים.
- שיוך הנציגות נבדק בנתונים ולא בהנחה: 29 מתוך 29 שנבדקו התאימו גם לפי
  מייל וגם לפי שם, ולכל הנציגים הפעילים יש מייל. המפתח הוא **מייל** — שם
  תצוגה ניתן לעריכה בכל אחת מהמערכות בנפרד.

### תשתית
- `public.zendesk_tickets` + `public.zendesk_sync_state`, עם RLS בהיקף
  מחלקה זהה ל-`calls`/`agents` (צופה משויך רואה רק פניות של נציגי מחלקתו).
- Edge Function `sync-zendesk-tickets` המשתמשת ב-**Incremental Export** של
  Zendesk ולא ב-Search: ה-Search חסום ב-1000 תוצאות והסמן שלו נודד, בעוד
  שה-Incremental מדפדף אמין ומאפשר side-load של המשתמשים — כך שם וטלפון
  הלקוח מגיעים יחד עם הפנייה במקום קריאת API נפרדת לכל אחת.
- הסמן נשמר ב-`zendesk_sync_state` ולא נגזר מ-`max(updated_at)`, כדי
  שריצה שנכשלה לא תדלג בשקט על פניות שהשתנו בזמן שהייתה מושבתת.
- סנכרון ראשוני: 12 עמודים, 11,359 פניות נסרקו, **8,108 נשמרו**
  (שנוצרו מ-1 באוגוסט). 309 פתוחות, 7,779 סגורות, 99.9% עם טלפון.
- הכול **תוספתי** — לא נגעתי בטבלה, מדיניות או פונקציה קיימת.
- קבצים: `supabase/migrations/20260806140000_zendesk_tickets_tracking.sql`,
  `supabase/migrations/20260806141000_schedule_zendesk_tickets_sync.sql`,
  `supabase/functions/sync-zendesk-tickets/index.ts`, `src/lib/tickets.ts`,
  `src/app/api/zendesk-tickets/route.ts`,
  `src/components/ticket-tracking-page.tsx`,
  `src/app/ticket-tracking/page.tsx`, `src/lib/app-pages.ts`,
  `src/components/sidebar.tsx`.

---

## [2026-08-06] — זמני שיחות בעמוד "יעדים לנציגים"

### מוצר
- נוספו שתי עמודות: **"זמן שיחות נכנסות"** ו-**"זמן שיחות יוצאות"**, לכל
  נציג פר מחלקה, בפורמט שעות:דקות, לאותה תקופה שנבחרה בסינון התאריך.
  הסכומים מופיעים גם בכותרת של כל מחלקה.

### הגדרה
- הזמן הוא `talk_time_seconds` ולא `duration_seconds`: ביוצאות ה-duration
  כולל גם חיוג וצלצול, כך שנציג שמחייג ל-30 מספרים שאיש לא עונה בהם היה
  מוצג עם שעות של "זמן שיחות" בלי שדיבר עם אף אחד.
- בשונה מעמודת "בפועל", עמודות הזמן **כן** כוללות שיחות שהנציג העביר
  הלאה — היעד מודד זכאות לטיפול, הזמן מודד כמה זמן הנציג באמת היה על הקו.

### תשתית
- `public.agent_target_report` מחזירה שתי עמודות נוספות. בוצע DROP+CREATE
  ולא `CREATE OR REPLACE` כי Postgres לא מאפשר לשנות טיפוס החזרה במקום;
  רשימת הפרמטרים לא השתנתה, ולכן ה-Edge Function של המייל היומי והעמוד
  ממשיכים לקרוא לה בדיוק כמו קודם.
- אומת מול נתוני אמת שההתנהגות הקיימת לא נדרסה: `inbound_answered`
  זהה לספירה ישירה (79=79), וזמני הנכנסות והיוצאות תואמים סכום ישיר
  (25,295 ו-5,207 שניות).
- נוסף `formatHoursMinutes` ב-`agent-targets.ts` — `formatDuration` הקיים
  מחזיר mm:ss שמתאים לשיחה בודדת, לא לסך יומי.
- קבצים: `supabase/migrations/20260806090000_agent_target_report_talk_time.sql`,
  `src/lib/agent-targets.ts`, `src/app/api/agent-targets/route.ts`,
  `src/components/agent-targets-page.tsx`, `src/lib/agent-targets.test.ts`.

---

## [2026-08-03] — קווים שהוצאו משימוש מפסיקים להיספר

### אבחון — "שיחות ממתינות שלא מצלצלות אצל אף נציג"
- דווח: 3 שיחות ממתינות ~25 דקות בזמן שיש נציגים זמינים באספקות.
- **לא תקלת webhook ולא באג בדשבורד.** ה-webhook חי (114 כתיבות ב-30
  דקות, שיחות אחרות עוברות מחזור מלא), והדשבורד הציג נכון מצב אמיתי.
- הסיבה: השיחות נכנסו לקו Aircall `"אספקות - ישן"`
  (`+972 55-339-0267`, line 1325898). ב-payload: `"teams": []` אבל
  `"availability_status": "open"` — המספר פתוח ומקבל שיחות, אין אליו צוות,
  ולכן Aircall עונה, מנגן מוזיקת המתנה ולא מצלצל אצל איש. האירוע היחיד
  שנרשם הוא `call.created`.
- ראיה משלימה מאותו יום: בקו הישן 3 שיחות / 0 נענו / 3 תקועות; בקו החי
  `אספקות` (`+972 55-338-6237`) 302 שיחות / 204 נענו / 0 תקועות.
- רקע: אותו line_id היה הקו הפעיל עד 26/07 (1,843 נכנסות, 1,391 נענו).
  במעבר למספרים החדשים הקווים שונו ל"- ישן" והצוותים הוסרו, אבל הם
  נשארו פתוחים. אותו סיפור ב-`"שירות - ישן"` (`+972 55-330-1759`).
- שני מסלולים מגיעים לשם: לקוחות עם המספר הישן שמור, **וגם העברות של
  נציגים** — ב-09:09:17 בוצעה העברה חיצונית, ושנייה קודם אותו לקוח הופיע
  כשיחה תקועה חדשה על הקו הישן.
- השורות התקועות נסגרות מעצמן אחרי 30 דקות ע"י
  `private.sweep_stale_open_calls` ומסומנות `missed`.
- **התיקון האמיתי הוא ב-Aircall** — לסגור/לנתב את שני המספרים הישנים
  ולתקן את יעד ההעברה של הנציגים. הסינון למטה מסתיר, לא פותר.

### תשתית — `talk_lines.excluded_from`
- נוספה עמודה `excluded_from timestamptz` ל-`talk_lines`: כשהיא מוגדרת,
  שיחות על הקו שהתחילו ממנה והלאה לא נספרות ולא מוצגות. הוגדרה
  `2026-07-26 15:00:00+00` לשני הקווים הישנים.
- הגבול הוא מועד המעבר ולא "עכשיו", כדי שכל התעבורה מהתקופה שבה אלה היו
  הקווים החיים תישאר. בפועל: **74 שיחות הוסתרו, 6,576 נשמרו.**
- `department_lines` לא שונה בכוונה — המיפוי הוא מה שהטביע `department_id`
  על השורות ההיסטוריות, וסנכרון חוזר חייב לפתור אותו זהה.
- הסינון הוחל ב-`/api/dashboard` (מזין את כל המסכים) וב-
  `public.agent_target_report` (העמוד והמייל היומי), כתנאי אחד לכל קו:
  לשמור שורה אלא אם היא על אותו קו **וגם** התחילה אחרי ה-cutoff שלו.
- קבצים: `supabase/migrations/20260803120000_exclude_retired_lines.sql`,
  `src/app/api/dashboard/route.ts`.

---

## [2026-08-02] — יעדים לנציגים: הגדרה, עמוד מעקב ודוח יומי במייל

### מוצר — פיצ'ר חדש
- טאב חדש בהגדרות, **"יעדים לנציגים"**: מטריצה של נציגים פעילים מול
  מחלקות, יעד שיחות נכנסות יומי לכל תא, כולל רשימת תפוצה למייל ומתג
  הפעלה/כיבוי לדוח. נשמרים רק תאים שהשתנו.
- עמוד חדש **"יעדים לנציגים"** (`/agent-targets`): בחירת תאריך, פירוט פר
  מחלקה ופר נציג — יעד, בפועל, פער ואחוז עמידה. ברירת המחדל היא החתך
  שנשלח במייל (00:00–15:30), עם אפשרות "יום מלא".
- דוח יומי במייל ב-15:30 שעון ירושלים, עברית מלאה RTL, מקובץ לפי מחלקה
  עם שורת סה״כ לכל מחלקה.

### הגדרת המדד (סוכם מול הלקוח)
- נספרות שיחות `direction='inbound'` + `status='answered'` המשויכות
  לנציג, **בניכוי** שורות שבהן `transferred_by_agent_id = agent_id`.
- הרקע: Aircall מתעד שני סוגי העברה אחרת לגמרי. בהעברה **פנימית**
  הבעלות עוברת ו-`agent_id` נעשה נציג היעד — הנציג המעביר יוצא מהספירה
  מעצמו (17 שורות בנתונים). בהעברה **חיצונית** הנציג נשאר `agent_id`
  ומסומן גם כמעביר — 503 שורות, ~11% מהנכנסות שנענו; אלו השורות
  שהסינון מנכה. שיחה שהועברה **אל** הנציג כן נספרת לו.
- שיוך לפי `department_id` של **השיחה**, לא של הנציג: 8 נציגים עונים
  לשתי המחלקות ולכל אחת מהן יעד נפרד.
- אימות מול נתוני אמת (02/08): 256 נכנסות שנענו − 35 העברות עצמיות = 221,
  זהה לסכום שמחזירה הפונקציה.

### תשתית
- מיגרציה `20260802100000_agent_call_targets.sql`: טבלאות
  `agent_call_targets` / `agent_target_report_settings` /
  `agent_target_report_recipients` + RLS (קריאה לפי מחלקה כמו
  `20260730200000`, כתיבה לאדמין בלבד), ופונקציה
  `public.agent_target_report(from, to)` — מקור אמת יחיד למדד, כך
  שהעמוד והמייל לא יכולים להיפרד. `SECURITY INVOKER` כדי שהיקף המחלקה
  יחול על קריאות מהאפליקציה.
- Edge Function `agent-targets-report` (+ `email.ts` נפרד לתבנית, כדי
  שניתן יהיה לרנדר ולבדוק אותה לבד). `{"force":true}` שולח מייל מבחן.
- `20260802110000_schedule_agent_targets_report.sql` — **טרם הוחל**.
  ה-cron רץ ב-12:30 וב-13:30 UTC כי ל-pg_cron אין אזור זמן ו-15:30
  בירושלים הוא 12:30 בקיץ ו-13:30 בחורף; הפונקציה מדלגת לפני שעת היעד
  או אם כבר נשלח היום, כך שאין כפילות ואין תחזוקת DST.
- `jerusalemDayBounds` הוכלל ל-`jerusalemInstant(date, time)` ב-
  `israel-time.ts` במקום שכפול הלוגיקה.
- קבצים: `src/lib/agent-targets.ts`, `src/lib/israel-time.ts`,
  `src/app/api/agent-targets/route.ts`,
  `src/app/api/settings/agent-targets/route.ts`,
  `src/components/agent-targets-settings.tsx`,
  `src/components/agent-targets-page.tsx`,
  `src/app/agent-targets/page.tsx`, `src/lib/app-pages.ts`,
  `src/components/sidebar.tsx`, `src/components/settings-tabs.tsx`,
  `src/lib/agent-targets.test.ts`.

---

## [2026-07-30] — צמצום סערת הבקשות מ-/calls ו-/agents (שורש ה-403)

### תשתית — עומס שגרם לחסימת IP ב-LiteSpeed
- אובחן: הדומיין `zend-shirot.rc-info.org` (Hostinger hcdn → LiteSpeed
  ב-31.97.121.216) תקין לחלוטין; דף ה-403 מוגש **לפי IP** ע"י הגנת
  ההצפה של LiteSpeed, לפני האפליקציה.
- הסיבה: `useDashboardData` ב-`section-pages.tsx` נקרא ללא טווח ב-/calls
  וב-/agents, כלומר ברירת המחדל של ה-API (31 יום ≈ 12,000 שורות, עד 14
  שאילתות DB), ונשלף **כל 15 שניות + בכל אירוע Realtime בודד** בכל טאב
  פתוח. במשרד עם כמה טאבים זה מגיע מ-IP אחד.
- תוקן: טווח ארוך משבוע עובר ל-polling של 60 שניות ובלי Realtime; אירועי
  Realtime בטווח חי מקובצים (debounce 3 שניות) במקום refetch לכל שורה;
  נוסף רצף בקשות (requestId) שמונע דריסת תגובה חדשה בישנה; `/agents`
  מבקש טווח של היום בלבד במקום 31 יום (הוא כלל לא משתמש בשיחות);
  טבלת היסטוריית השיחות מוגבלת ל-500 שורות ב-DOM עם ציון מפורש,
  ו-`truncated` מוצג גם שם.
- קבצים: `src/components/section-pages.tsx`.

---

## [2026-07-30] — האצת שבוע/חודש בעמוד ניטור בזמן אמת

### ביצועים — טעינה ורינדור של טווחים ארוכים
- **API** (`/api/dashboard`): עמודי השיחות נטענים כעת במקבצים מקבילים של 4
  במקום 12 קריאות עוקבות — טעינת חודש ירדה מ-12 סבבי רשת ל-3. נשמר מסלול
  סדרתי כ-fallback אם `db-max-rows` יוגדר אי-פעם מתחת לגודל עמוד.
- **דשבורד**: הוסר טיימר גלובלי שרינדר מחדש את כל העמוד כל שנייה (כולל
  טבלה של אלפי שורות בטווח חודש — זה מה שהקפיא את הדף). שעוני "זמן שחלף"
  הם עכשיו קומפוננטת `Elapsed` עצמאית שמרנדרת רק את עצמה.
- הטבלה ממקסמת 500 שורות ב-DOM (החיפוש עדיין סורק את כל הטווח) ומוצגת
  הערה כשמדובר בפרוסה חלקית / טווח שנחתך ב-12,000.
- טווח ארוך משבוע: polling כל 60 שניות במקום 15, ובלי רענון Realtime על
  כל אירוע שיחה — גם חוויית שימוש וגם הורדת עומס מהשרת (אותו דפוס עומס
  שהפעיל את חסימת ה-403 של LiteSpeed).
- lint: הוחרג `.deploy-payloads/` (קבצי עזר מקומיים); תוקן דפוס
  setState-in-effect בטעינת היסטוריה ב-agent-ai-analysis.
- קבצים: `src/app/api/dashboard/route.ts`,
  `src/components/dashboard-client.tsx`,
  `src/app/agent-ai-analysis/agent-ai-analysis-client.tsx`,
  `eslint.config.mjs`.

---

## [2026-07-30] — אבחון 403 בפרודקשן, תיעוד אחסון אמיתי, הקשחת RLS מחלקתי

### אבחון תקלה (ללא שינוי קוד אפליקציה)
- אומת שהפרודקשן רץ על **Hostinger/LiteSpeed (31.97.121.216)** ולא על Vercel,
  ושאין שום auto-deploy מ-GitHub — לכן תיקון הסינון (`7b4f36b`) **טרם פרוס**.
  דף ה-403 שהמשתמשים רואים הוא דף ברירת המחדל של LiteSpeed (הגנת עומס לפי IP),
  ותואם לסערת הבקשות שיצרה הרגרסיה של הפדפוד. פירוט ב-`PROJECT_CONTEXT.md`.
- כשל `gemini_empty_response` של מור אלקיים פוענח: `PROHIBITED_CONTENT` על
  אודיו של שיחה אחת (3999276611). v15 דילג עליה והדוח ל-28.07 הושלם (46 שיחות,
  ציון 7, skipped=1) — אין צורך בהרצה חוזרת.

### אבטחה — הקשחת RLS מחלקתי (הופעל)
- מיגרציה `20260730200000_department_scoped_rls.sql` סוגרת את
  `using (true)` על calls / call_legs / call_recordings / agents /
  agent_live_status / agent_status_history ומחליפה במדיניות לפי מחלקת
  המשתמש (זהה ל-`department-scope.ts`). **הופעלה מול Supabase החי
  ב-2026-07-30** ואומתה ב-SQL בסימולציית תפקידים: צופה משויך רואה רק את
  מחלקתו (0 דליפות שיחות/נציגים), אדמין ומנהל ללא מחלקה רואים הכל.
  נותר להמשך: `queue_snapshots`, `zendesk_customers`.

---

## [2026-07-23] — פיצול דף Settings לטאבים לפי קטגוריה

### UI
- במקום חמישה כרטיסים מוערמים — ארבעה טאבים (שיחות שלא נענו, שעות
  פעילות, AI, Aircall) דרך `SettingsTabs` בצד הלקוח.
- קבצים: `src/app/settings/page.tsx`, `src/components/settings-tabs.tsx`.

---

## [2026-07-23] — ניקוי כתובת שולח מה-seed של התראות missed-call

### הגדרות
- שורת ה-bootstrap seed נוקתה מכתובת שולח של לקוח ספציפי (`null`) —
  התקנה חדשה (כולל replay מלא של מיגרציות) מתחילה לא-מוגדרת ודורשת
  הזנה ושמירה ב-Settings.
- קבצים: `supabase/migrations/20260723074747_clear_missed_call_notification_from_email_seed.sql`.

---

## [2026-07-23] — בלי כתובת שולח קשיחה ב-notify-missed-call

### אבטחה / הגדרות
- הוסר fallback לכתובת שולח hardcoded ב-Edge Function; גם ברירת המחדל
  בעמודת DB הוסרה. בלי שולח מוגדר בהגדרות — לא נשלח מייל.
- קבצים: `supabase/functions/notify-missed-call/index.ts`,
  `supabase/migrations/20260723073859_missed_call_notification_from_email_no_default.sql`.

---

## [2026-07-23] — התראות מייל על שיחות שלא נענו (Resend)

### תשתית התראות
- אדמין מגדיר ב-Settings כתובת שולח ורשימת נמענים להתראות על שיחות
  `missed`.
- טריגר על `public.calls` שולח פעם אחת כששיחה הופכת ל-`missed` ל-Edge
  Function חדשה `notify-missed-call`, שמיישמת את אותו סף "מענה קצר /
  לא-נענה" כמו בשאר האפליקציה, ואז שולחת מייל HTML בעברית RTL דרך Resend.
- קבצים: `src/app/settings/page.tsx`,
  `src/app/api/settings/missed-call-notifications/`,
  `src/components/missed-call-notification-settings.tsx`,
  `supabase/functions/notify-missed-call/`,
  `supabase/migrations/20260723073102_missed_call_email_notifications.sql`.

---

## [2026-07-23] — התעלמות מקבצי IDE מקומיים ב-git

### Repo hygiene
- `.claude/` ו-`.mcp.json` נוספו ל-`.gitignore` כדי ש-`git status` יישאר נקי
  ולא ידחוף קונפיג מקומי של Cursor/Claude.
- קבצים: `.gitignore`.

---

## [2026-07-23] — שיחות שלא נענו: הצגת זמן המתנה במקום Talk Time

### UI — היסטוריית שיחות
- בשיחה `missed` עמודת משך מציגה כעת **המתנה** (`waitTimeSeconds`) במקום
  משך דיבור (שאינו רלוונטי כשלא הייתה מענה).
- קבצים: `src/components/section-pages.tsx`.

---

## [2026-07-22] — לוג מלא לכל webhook נכנס ב-aircall-webhook

### Observability
- בכל קבלת POST ל-`aircall-webhook` נכתב כעת לוג Edge עם סוג האירוע
  וה-payload המלא (השדה `token` מוסתר). גם גוף JSON לא-תקין נרשם.
- קבצים: `supabase/functions/aircall-webhook/index.ts`.

---

## [2026-07-22] — תיקון סטטוס נציג תקוע/שגוי במסך מוקד (TV)

### באג — ארכיטקטורה של כתיבות כפולות/מתפצלות לסטטוס נציג
- מנהלים דיווחו שסטטוס נציג במסך המוקד (TV) לא מתעדכן כשהוא משתנה
  בפועל ב-Aircall (למשל נכנס לשיחה, משנה נוכחות).
- הסיבה: היו **6 כותבים עצמאיים** לעמודת `agent_live_status.state`, עם
  **3 עותקים נפרדים ומתפצלים** של לוגיקת "תרגום סטטוס Aircall לסטטוס
  פנימי" — `mapAvailability` ב-webhook, `mapState` ב-cron
  `sync-aircall-users` (רץ כל דקה ללא תלות באירועים), ו-
  `private.aircall_state_from_user()` ב-SQL (נקרא משני triggers).
  אומתו אי-התאמות בפועל: `"custom"` ממופה ל-`available` בעותק אחד
  ול-`scheduled` בשניים אחרים; ל-`mapState` חסרים מפתחות נרדפים
  שקיימים בעותקים האחרים — ומכיוון שהוא רץ כל 60 שניות ללא קשר
  לאירועים, ערך לא-מזוהה שם גורם לסטטוס שגוי שנכפה מחדש כל דקה. טריגר
  SQL נוסף (`apply_aircall_user_status`) התברר ככפילות מוחלטת של מה
  שהוובהוק כבר כתב רגע קודם באותה בקשה.
- תוקן ע"י איחוד הלוגיקה למקור אמת יחיד: `mapAvailability()` חדש ב-
  `supabase/functions/_shared/aircall-status.ts`, בשימוש הן ע"י
  ה-webhook והן ע"י ה-cron. שני ה-triggers המיותרים ב-SQL
  (`apply_aircall_user_status`, `guard_aircall_agent_status_order`)
  הוסרו במיגרציה חדשה. **החלטת מוצר:** סטטוס "Custom"/לא-מזוהה מוצג
  כעת כ-`other` ("אחר") במקום נדחס בטעות ל"זמין" או "לפי לוח".
- קבצים: `supabase/functions/_shared/aircall-status.ts` (חדש),
  `supabase/functions/aircall-webhook/index.ts`,
  `supabase/functions/sync-aircall-users/index.ts`,
  `supabase/migrations/20260722140000_retire_duplicate_agent_status_triggers.sql`.

---

## [2026-07-22] — תיקון "gemini_empty_response" בניתוח AI לנציג

### באג — ניתוח יום נציג נעצר ב-~80% עם שגיאת gemini_empty_response
- מנהלים דיווחו שניתוח AI ליום עבודה של נציג (`/agent-ai-analysis`) מתקדם עד כ-80% ואז נכשל עם השגיאה `gemini_empty_response` (HTTP 500 מ-`analyze-agent-day`).
- הסיבה: הקריאה ל-`gemini-2.5-pro` לא הגבילה `maxOutputTokens`/`thinkingConfig.thinkingBudget`. ה"חשיבה" הפנימית של המודל נספרת מתוך תקציב הפלט — באצווה עם שיחות ארוכות/Transfer/Hold המודל יכול "לגמור" את כל התקציב על חשיבה ולהחזיר תשובה ריקה (`finishReason: MAX_TOKENS`), בדיוק ליד סוף רשימת האצוות (סביב 80%-90% התקדמות).
- תוקן ע"י: הגבלת `thinkingConfig.thinkingBudget` לערך נמוך וקבוע (1024) והגדרת `maxOutputTokens` נדיב (16384) כדי שתמיד יישאר מקום לתשובת ה-JSON בפועל; נוסף retry עם backoff (עד 2 ניסיונות חוזרים) על `gemini_empty_response` ועל שגיאות 429/5xx חולפות — אותו דפוס retry שכבר קיים ב-`_shared/zendesk.ts`.
- קבצים: `supabase/functions/analyze-agent-day/index.ts`.

---

## [2026-07-22] — פיצ'ר: סף "לא נענה פחות זמן" (ניתן להגדרה)

### פיצ'ר חדש
- מנהל יכול להגדיר בעמוד ההגדרות סף המתנה בשניות (גלובלי, ברירת מחדל 60). שיחות נכנסות שלא נענו וזמן ההמתנה של הלקוח בהן היה מתחת לסף מסווגות לתצוגה כ"לא נענה פחות זמן" — לא נספרות כ"לא נענו" ולא משפיעות על אחוז המענה (כללי או פר-מחלקה/נציג), אך ממשיכות להופיע בכל מקום במערכת. `calls.status` ב-DB לא משתנה — סיווג נגזר/display-only בלבד (`isShortNoAnswer()` ב-`metrics.ts`), כך שנתוני עבר לא נמחקים ולא משתנים.
- **חריג מכוון:** מסך מוקד (TV) (`/wallboard`) ממשיך להציג את מונה "לא נענו" המקורי (כולל שיחות קצרות) — לא עבר את הסינון החדש, לפי בקשה מפורשת (הכי חשוב שם יציבות/פשטות, לא הפילוח).
- טבלה חדשה `missed_call_settings` (singleton row, RLS admin-write) — קובץ migration בלבד, טרם הורץ מול Supabase החי.
- מחוץ להיקף: ה-Edge Function `analyze-agent-day` (עמוד "ניתוח AI לנציגים") לא עודכן — runtime נפרד, ידרוש שינוי נפרד בהמשך אם יידרש.
- קבצים עיקריים: `supabase/migrations/20260722120000_missed_call_threshold.sql`, `src/app/api/settings/missed-call-threshold/route.ts`, `src/hooks/use-missed-call-threshold.ts`, `src/components/missed-call-threshold-settings.tsx`, `src/lib/metrics.ts`, `src/lib/types.ts`, `src/lib/excel-export.ts`, `src/components/section-pages.tsx`, `src/components/dashboard-client.tsx`, `src/app/after-hours/after-hours-client.tsx`.

---

## [2026-07-22] — תיקון תנודתיות במונה "לא נענו" במסך מוקד (TV)

### באג — Race condition בטעינת נתונים
- מנהלות דיווחו שהתא "לא נענו" במסך המוקד (`/wallboard`) מציג לפעמים ערך שונה בין רענון לרענון (למשל 17 ואז 16) בלי שהנתונים באמת השתנו.
- הסיבה: `loadData()` נקרא בו-זמנית ממספר מקורות (פולינג כל 10 שניות + מנוי Realtime של Supabase על `calls`/`agent_live_status`/`agents`), בלי הגנה על סדר התגובות. תגובת fetch "ישנה" שחוזרת מהרשת אחרי תגובה "חדשה" הייתה דורסת אותה ב-`setData`.
- תוקן ע"י הוספת `latestRequest` ref שמסמן מספר סידורי לכל בקשה ומוודא שרק תגובת ה-fetch העדכנית ביותר מעדכנת את ה-state — אותו דפוס שכבר קיים ועובד ב-`dashboard-client.tsx`.
- קבצים: `src/components/wallboard-client.tsx`.

---

## [2026-07-21] — CHANGELOG חובה + כלל Cursor

- נוצר `CHANGELOG.md` כיומן שינויים מרכזי (מהיום והלאה + סיכום יולי 2026).
- נוסף כלל תמיד-פעיל: `.cursor/rules/update-changelog.mdc` — חובה לעדכן את ה-CHANGELOG בכל שינוי משמעותי.
- `PROJECT_CONTEXT.md` עודכן (קישור ל-CHANGELOG, route `/agent-ai-analysis`, `analyze-agent-day`, טבלת `agent_day_analyses`).

---

## [2026-07-21] — ניתוח AI לשיחות/נציגים, Hold/Transfer, סטטוס לייב

### AI — Hold + Transfer בהקלטות
- ניתוח Gemini מודע ל-Hold ול-Transfer: לא לשפוט רעשי מוקד בזמן Hold; בשיחות שהועברו — לנתח רק את הנציג המנותח.
- חילוץ חלונות Hold ואירועי Transfer מ-`call_raw` / webhook.
- קבצים: `supabase/functions/_shared/recordings.ts`, `analyze-recording`, `analyze-agent-day`, `aircall-webhook`.

### AI — ניתוח יומי לנציג + היסטוריה
- דף חדש `/agent-ai-analysis` (אדמין + דגל `ai_call_analysis`).
- Edge `analyze-agent-day` + טבלה `agent_day_analyses` (היסטוריית ניתוחים).
- הרשאות סיידבר / `app-pages` / ניהול משתמשים.
- קבצים: `src/app/agent-ai-analysis/*`, `src/app/api/agent-ai-analysis/route.ts`, מיגרציה `20260721160000_agent_day_analyses_history.sql`.

### חיפוש טלפון
- נרמול מקומי/בינלאומי בחיפוש מספרים.
- מיגרציה: `20260721150000_phone_search_local_intl_normalization.sql`.

### סטטוס Aircall / Wallboard (תיקוני ייצור)
- סגירת שיחות `in_progress` תקועות תוך כיבוד Away (Back office וכו').
- Reconciliation ל-`on_call` משיחות פעילות; תזמון reconciliation; חיזוק טריגר סטטוס.
- מיגרציות: `20260721103000_*`, `20260721120000_*`, `20260721130000_*`, `20260721140000_*`.
- קבצים: `aircall-webhook`, `sync-aircall-users`, `src/app/api/dashboard/route.ts`, `wallboard-client.tsx`.

### שעות פעילות / After-hours / ייצוא
- עדכוני business hours, דף after-hours, הקלטות, excel export, הגדרות AI/business-hours API.

---

## [2026-07-20] — הקלטות, AI בסיסי, העברות, שעות מחלקה

- Pagination RPC להקלטות (`list_call_recordings_page`); רענון URL מ-Aircall ב-403.
- Feature flag `ai_call_analysis` + דף `/ai-analysis` + Edge `analyze-recording`.
- מעקב העברות שיחה (`call_transfer_tracking`).
- שעות פעילות למחלקה (`department_business_hours`).
- היסטוריית סטטוס נציגים + לוג מערכת.
- ניהול משתמשים / פרופילים.

---

## [2026-07-19] — מעבר ל-Aircall + בסיס הדשבורד

- Bootstrap ראשוני (Zendesk-era) ואז מעבר ל-Aircall כמקור לייב.
- Webhook Aircall, מחלקות/קווים/צוותים, סאב-סטטוסים רשמיים, roster sync.
- הקלטות, Realtime agents, אבטחה ו-RLS, מניעת overwrite של סטטוס שיחה ישן.
- כיבוי jobs של Zendesk כשהמקור העיקרי הוא Aircall.

---

## תבנית לכניסה חדשה

```markdown
## [YYYY-MM-DD] — כותרת קצרה

### נושא
- מה השתנה ולמה.
- קבצים / מיגרציות / Edge Functions רלוונטיים.
```
